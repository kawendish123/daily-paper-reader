---
title: Fast Transformer Inference on ARM-Based HMPSoCs
title_zh: 基于ARM的HMPSoCs上的快速Transformer推理
authors: "Hang Xu, Yixian Shen, Thanassis Giannetsos, Anuj Pathania"
date: 2026-06-01
pdf: "https://arxiv.org/pdf/2606.02836v1"
tags: ["query:mec-video"]
score: 8.0
evidence: 边缘设备上的快速Transformer推理
tldr: "Transformer模型已成为机器学习任务的新标杆，但其在资源受限的边缘设备上部署仍面临挑战。ARM Compute Library (ARM-CL) 主要用于低延迟CNN推理，缺乏对Transformer的原生支持。本文通过实现新的Transformer内核扩展了ARM-CL，使在ARM HMPSoC上的推理速度比最先进的CPU/GPU实现提升最高3倍。此外，通过利用HMPSoC的CPU和GPU进行协同推理，将内存密集型操作置于CPU，计算密集型操作置于GPU，进一步将推理延迟降低15.72%。该工作为边缘端高效Transformer推理提供了新方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.02836v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 910, \"height\": 524, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02836v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1602, \"height\": 682, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02836v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1740, \"height\": 922, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02836v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 738, \"height\": 340, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02836v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 911, \"height\": 848, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.02836v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 914, \"height\": 416, \"label\": \"Figure\"}]"
motivation: 现有ARM-CL缺乏Transformer推理支持，且单处理器无法充分利用HMPSoC异构资源，导致边缘推理延迟高。
method: 在ARM-CL中实现Transformer新内核，并设计CPU-GPU协同策略，按操作类型分配处理器。
result: "扩展ARM-CL实现3倍加速，协同推理进一步降低延迟15.72%。"
conclusion: 本文实现了ARM平台上高效Transformer推理，验证了异构协同的有效性。
---

## 摘要
Transformer模型为机器学习（ML）任务设立了新的性能标准。然而，在资源受限的边缘设备上进行无云端、片上Transformer推理的资源密集型部署仍然具有挑战性。ARM计算库（ARM-CL）框架为基于ARM的边缘设备提供了低延迟的CNN推理，但缺乏对Transformer推理的支持。在这项工作中，我们在ARM-CL中实现了多个新的Transformer内核，以支持原生Transformer执行。与基于ARM的嵌入式板上的最先进CPU/GPU实现相比，我们扩展的ARM-CL实现了高达三倍的Transformer推理加速。此外，为边缘设备提供动力的异构多处理器片上系统（HMPSoCs）同时提供了嵌入式CPU和GPU。我们引入了协作式CPU-GPU Transformer推理，该方法在CPU上执行内存密集型操作，同时利用GPU进行高度可并行化、计算密集型操作。这种协作执行的开销极小，与ARM-CL上最佳单处理器推理相比，进一步将Transformer推理延迟降低了高达15.72%。

## Abstract
Transformer models have set new performance standards for machine learning (ML) tasks. However, their resource-intensive deployment on resource-constrained edge devices for cloud-free, on-chip transformer inference remains challenging. The ARM Compute Library (ARM-CL) framework provides low-latency CNN inference on ARM-based edge devices but lacks support for transformer inference. In this work, we implement several new transformer kernels in ARM-CL to support native transformer execution. Our extended ARM-CL achieves up to three times faster transformer inference compared to state-of-the-art CPU/GPU implementations on an ARM-based embedded board.   Furthermore, heterogeneous multi-processor system-on-chips (HMPSoCs) powering edge devices provide both embedded CPUs and GPUs. We introduce cooperative CPU-GPU transformer inference, which executes memory-intensive operations on the CPU while utilizing the GPU for highly parallelizable, compute-intensive operations. This cooperative execution, implemented with minimal overhead, further reduces transformer inference latency by up to 15.72% compared to the best single-processor inference on ARM-CL.

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）
- **背景**：Transformer模型在自然语言处理、视觉等任务中表现卓越，但在资源受限的边缘设备上进行片上推理面临高延迟、高计算量和内存带宽瓶颈。边缘设备普遍采用ARM架构的异构多处理器片上系统（HMPSoC），集成了嵌入式CPU和GPU。
- **问题**：现有的ARM Compute Library (ARM-CL) 仅针对CNN推理进行了优化，缺乏对Transformer所需操作（如自注意力、层归一化等）的原生支持。同时，通用编译框架（如TVM）在ARM硬件上性能不佳，PyTorch等框架不支持ARM Mali GPU。
- **动机**：要实现低延迟的Transformer片上推理，必须充分利用HMPSoC的CPU和GPU异构计算能力。本文旨在通过扩展ARM-CL并设计CPU-GPU协同推理策略，显著降低Transformer推理延迟。

### 2. 方法论
- **核心思想**：扩展ARM-CL库，添加Transformer专属内核；基于层级别性能异质性分析，将不同的Transformer层分配给最适合的处理器（CPU或GPU），实现CPU-GPU层切换协同推理。
- **关键技术细节**：
  - **内核扩展**：在ARM-CL中实现了Embedding、Multi-Head Attention (MHA)、Scaled Dot-Product Attention (SDPA)、Layer Normalization等新内核。CPU内核使用NEON SIMD指令，GPU内核使用OpenCL（如分块矩阵乘法）。
  - **性能异质性分析**：通过微基准测试，分别测量五种Transformer层（Embedding、Attention Linear、SDPA、Feed Forward、Add & Norm）在CPU和GPU上的执行时间。结果发现：Embedding、SDPA、Add & Norm等内存密集型层在CPU上更快；Attention Linear和Feed Forward等计算密集型层在GPU上更快，但长序列时GPU优势减弱（因缓存限制）。
  - **CPU-GPU协同策略**：利用HMPSoC的共享内存空间，在CPU和GPU之间传递张量时无需显式memcpy，仅需OpenCL的map/unmap操作。将层依次分配给最优处理器，仅在不同处理器交界处使用多目标共享张量。
  - **实现流程**：将Transformer模型编译为图结构，根据预定义的层分配策略（基于图3的微基准映射）将各个层静态分配到CPU或GPU，运行时由ARM-CL调度执行。

### 3. 实验设计
- **硬件平台**：Khadas VIM 3 BASIC板，搭载Amlogic A331D HMPSoC（12nm），包括四核ARM Cortex-A73 big CPU（2.2 GHz）和四核ARM Mali G52 GPU（0.8 GHz）。
- **数据集/场景**：使用五种代表性的Transformer架构进行推理延迟测量，包括BERT-base、DistilBERT、MobileBERT、SqueezeBERT、GPT-2。默认输入token长度L=32，并在微基准分析中改变L（16~512）和隐藏维度d_model（192~960）。
- **基准对比**：
  - 与TVM v0.18、ExecuTorch（仅CPU）对比单处理器推理性能。
  - 单处理器（仅CPU或仅GPU）与多处理器（CPU-GPU协同）推理对比。
  - 微基准实验（图3）展示了不同层在不同d_model和L下的CPU/GPU相对性能。
- **评估指标**：端到端推理延迟（毫秒）。

### 4. 资源与算力
- **推理资源**：实验在单个嵌入式开发板上完成，使用板载CPU和GPU。GPU为ARM Mali G52（单GPU），没有使用多GPU或外部加速器。论文未涉及模型训练，仅进行推理性能评估。
- **算力说明**：文中未提供详细的算力指标（如FLOPs、功耗等），仅以延迟作为主要评价指标。资源约束方面，提及DRAM容量（2-4 GB）限制了BERT-large的部署。

### 5. 实验数量与充分性
- **实验数量**：主要包含三大类实验：
  1. **框架对比实验（图5）**：在5种Transformer模型上分别对比TVM、ExecuTorch和扩展ARM-CL的CPU/GPU延迟。
  2. **单处理器 vs 多处理器实验（图6）**：同样5种模型上的CPU、GPU、CPU-GPU协同延迟对比。
  3. **微基准分析（图3）**：覆盖6种不同L（16~512）和5种d_model（192~960），全面展示各层的性能异质性。
- **充分性**：实验涵盖了多种主流通用Transformer变体（BERT、DistilBERT、MobileBERT、SqueezeBERT、GPT-2），并包括不同序列长度和维度下的细粒度分析。对比方法包括TVM和ExecuTorch两个主流边缘推理框架，且实验条件（如固定token长度）已明确，结果客观。但缺少与硬件加速器（如NPU）的对比，也未进行消融实验（如去掉协同策略的影响）。总体而言，实验设计较充分，结论可信。

### 6. 主要结论与发现
- 扩展的ARM-CL在CPU和GPU上分别比TVM平均快2.34倍和2.23倍，推理延迟显著降低。
- 基于层级别性能异质性，CPU-GPU层切换推理在所有5种Transformer模型上均优于最佳单处理器推理，平均延迟降低10.95%，最高达15.72%。
- 发现GPU在长序列（L≥128）时因缓存限制而性能下降，导致计算密集型层（如Attention Linear和FF）的GPU优势减弱。
- 证明了在ARM HMPSoC上利用CPU和GPU异构协同加速Transformer推理的可行性和有效性。

### 7. 优点
- **硬件针对性优化**：基于ARM架构的NEON和OpenCL进行内核级优化，比通用编译框架（TVM、ExecuTorch）性能更优。
- **低开销协同机制**：利用共享内存避免显式数据拷贝，使层切换的通信开销几乎可忽略。
- **开源贡献**：代码公开于GitHub（MIT许可），便于复现和扩展。
- **详尽性能分析**：通过微基准揭示了不同Transformer层在CPU/GPU上的性能差异，并给出了定量解释（如缓存大小影响），具有指导意义。
- **方法通用性**：层分配原则可推广到其他ARM HMPSoC平台，不局限于特定硬件。

### 8. 不足与局限
- **硬件单一性**：所有实验仅在一个特定嵌入式板（Khadas VIM 3）上进行，结果可能无法完全代表其他ARM HMPSoC（如不同缓存大小、CPU/GPU核心数）。
- **GPU缓存限制**：由于GPU L2只有128 KB，长序列时计算密集型层的GPU优势变弱，导致层切换带来的收益减少。若GPU缓存更大，方法可能更有效。
- **缺乏与NPU/DLA对比**：未与嵌入式神经处理单元（如Edge TPU、NPU）比较，而这些加速器在特定任务上可能更具优势。
- **仅关注推理延迟**：未评估能源效率、吞吐量或模型精度（假设为浮点精确推理），也未涉及模型压缩（量化、剪枝）的联合优化。
- **静态层分配**：层分配策略基于离线微基准测试，未引入运行时动态调度，可能无法适应实时负载变化。
- **可扩展性验证不足**：仅测试了单batch、固定输入长度（L=32）的推理，未评估批处理或多请求场景下的性能。

（完）
