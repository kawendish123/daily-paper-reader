---
title: "ScalePredictor: Instance-aware Scale Learning for Accurate Quantization of Vision Transformers"
title_zh: "ScalePredictor: 实例感知的尺度学习用于视觉Transformer的精确量化"
authors: "Changjun Li, Runqing Jiang, Lian Xu, Ye Zhang, Qingyong Hu, Yulan Guo"
date: 2026-06-20
pdf: "https://arxiv.org/pdf/2606.21947v1"
tags: ["query:mec-video"]
score: 8.0
evidence: 面向视觉Transformer的实例感知量化，支持在边缘设备上高效部署
tldr: Vision Transformers部署于边缘设备面临高计算需求，现有后训练量化方法采用静态尺度，未考虑实例间激活分布差异导致次优。本文提出ScalePredictor动态量化框架，利用浅层激活范围与深层最优尺度的相关性，通过多项式近似同时生成所有量化尺度，避免即时校准。在ImageNet上该方法显著提升精度，以极小开销实现更优权衡，为ViT高效部署提供新方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.21947v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 601, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21947v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1272, \"height\": 493, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21947v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1261, \"height\": 626, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.21947v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1237, \"height\": 500, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.21947v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1274, \"height\": 1303, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21947v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1261, \"height\": 1410, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21947v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 512, \"height\": 477, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21947v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 462, \"height\": 180, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21947v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 928, \"height\": 264, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21947v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 670, \"height\": 176, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.21947v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 526, \"height\": 176, \"label\": \"Table\"}]"
motivation: 现有ViT后训练量化采用静态尺度，无法适应图像多样性导致的激活分布变化，量化精度受限。
method: 提出ScalePredictor，提取浅层激活范围，经泰勒多项式投影模块动态生成各层最优量化尺度，实现实例感知量化。
result: 在ImageNet上以极低计算开销超越多种先进PTQ方法，取得更优精度-效率权衡。
conclusion: 揭示了层间尺度相关性，通过高效多项式近似实现动态量化，为ViT边缘部署提供实用方案。
---

## 摘要
视觉Transformer在许多领域取得了显著成功，但由于其巨大的计算需求，在边缘设备上的部署仍然具有挑战性。训练后量化（PTQ）通过使用少量校准集压缩模型且训练开销极小，提供了一种有吸引力的解决方案。然而，现有的大多数PTQ工作采用统一应用于所有实例的静态量化范式。由于自然图像具有显著的多样性，激活分布在不同样本间差异很大，使得这些方法本质上并非最优。在本文中，我们提出ScalePredictor，一种用于ViTs精确高效量化尺度学习的动态量化框架。我们首先揭示了浅层激活的分布范围与深层最优尺度之间的隐藏相关性。基于此，我们开发了一种尺度学习机制，该机制集成了高效的范围提取方法，以在浅层阶段捕获稳健的范围统计量，然后将其输入到泰勒动机的多项式尺度投影模块，同时生成所有量化尺度。由于多项式逼近的高效性，ScalePredictor引入了微不足道的计算开销，同时避免了昂贵的即时校准。在ImageNet上的大量实验表明，ScalePredictor始终优于先前的PTQ方法，实现了更优的精度-效率权衡。代码和额外结果见补充材料。

## Abstract
Vision Transformers have achieved remarkable success in many fields, yet their deployment on edge devices remains challenging due to their substantial computational demands. Post-Training Quantization (PTQ) offers an attractive solution by compressing models using a small calibration set with minimal training overhead. However, most existing PTQ works adopt a static quantization paradigm that is uniformly applied to all instances. Given the substantial diversity of natural images, the activation distributions vary significantly across samples, making these methods inherently suboptimal. In this paper, we propose ScalePredictor, a dynamic quantization framework for accurate and efficient quantization scale learning of ViTs. We first reveal a hidden correlation between the distribution range of shallow-layer activations and the optimal scales of deeper layers. Based on this, we develop a scale learning mechanism that integrates an efficient range extraction approach to capture robust range statistics at the shallow stage, which are then fed into a Taylor-motivated polynomial scale projection module to generate all quantization scales simultaneously. With the efficiency of polynomial approximation, ScalePredictor introduces insignificant computational overhead while avoiding costly just-in-time calibration. Extensive experiments on ImageNet demonstrate that ScalePredictor consistently outperforms prior PTQ methods, achieving a more favorable accuracy-efficiency trade-off. Code and additional results are shown in the supplementary materials.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **问题**：Vision Transformers (ViTs) 在图像任务上表现优异，但计算量大，部署到边缘设备困难。后训练量化 (PTQ) 作为一种轻量压缩方案，通常使用少量校准数据即可实现模型压缩。
- **现有局限**：大多数 PTQ 方法采用静态量化（所有输入样本共享同一套量化参数），忽略了自然图像实例间显著的激活分布差异。这种样本无关的设计在低比特（如 W3/A3）下精度损失严重。
- **目标**：在保持 PTQ 低开销的同时，实现实例感知的动态量化，以提升低比特 ViT 的量化精度。

## 2. 论文提出的方法论：核心思想、关键技术细节
### 2.1 核心思想
- 发现浅层激活的**鲁棒范围**（robust range）与后续各层的最优量化尺度之间存在强相关性，该关系在不同架构和深度上一致存在。
- 利用这一相关性，构建一个从浅层统计量到所有层量化尺度的轻量映射，避免逐层运行时归约计算，同时实现实例自适应。

### 2.2 关键技术
1. **鲁棒范围提取**  
   - 从 Patch Embedding 输出（每个图像一次）中提取稳定的标量上下文 \( r(x) \)。  
   - 方法：将展平后的张量分成 \( K \) 个非重叠块，计算每块的局部 min/max，再对局部极值取平均，得到鲁棒的全局 min/max，最后 \( r(x) = \text{avg(max)} - \text{avg(min)} \)。  
   - 优势：\( O(N\cdot C) \) 复杂度，无需排序，抑制异常值干扰。  
   - 将 \( r(x) \) 裁剪至校准集上 0.1%~99.9% 分位数区间。

2. **泰勒动机多项式尺度预测器**  
   - 将最优尺度 \( s_l^*(x) \) 建模为 \( r(x) \) 的光滑函数 \( \Phi_l(r(x)) \)。  
   - 基于校准集期望 \( \bar{r} \) 处进行 d 阶泰勒展开，最终得到多项式形式：  
     \[
     s_l(x) = \sum_{k=0}^{d} c_{l,k} \cdot (r(x))^k
     \]  
   - 系数 \( c_{l,k} \) 可学习；初始化时常数项设为静态最优尺度，高阶系数为零。  
   - 复杂度：每层仅需 O(d) 乘加运算。

3. **校准与推理流程**  
   - **校准**：逐块进行，以块级重建损失最小化优化多项式系数（使用 straight-through estimator）。  
   - **推理**：只计算一次 \( r(x) \)，然后通过多项式为所有量化层生成实例专属尺度。  
   - 整个流程无需逐层张量归约，与静态 PTQ 开销相当。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：ImageNet（120万训练，5万验证，1000 类）。
- **模型架构**：7 种 ViT 变体：ViT-Small/Base, DeiT-Tiny/Small/Base, Swin-Small/Base。
- **位宽设置**：W2/A3, W3/A3, W4/A3。
- **对比方法**：
  - 静态 PTQ 基线：BRECQ, QDrop, AdaLog, I&S-ViT。
  - 动态量化基线：JIT Min/Max（逐层运行时 min/max）、JIT Percentile（逐层排序百分位）。
  - ScalePredictor 分别集成到上述四个基线中（替换其静态缩放策略）。
- **实现细节**：校准集随机采样 1024 张训练图像；鲁棒范围提取使用 K=32 块；多项式度 d=1 或 2；优化器使用直通估计器；权重量化沿用基线策略。

## 4. 资源与算力
- 文中提及：所有实验在 **单张 NVIDIA GeForce RTX 3090 GPU** 上使用 PyTorch 完成。
- 未明确给出训练时长或总 GPU 小时数。但从校准流程（仅优化多项式系数，逐块重建，1024 张图像）推断，校准时间应在分钟量级，开销极低。

## 5. 实验数量与充分性
- **实验充分性**：非常充分。主实验（Table 1）涵盖 7 种架构 × 3 种位宽 × (4 基线 + 2 JIT) ≈ 130 组结果，且展示了两大趋势：ScalePredictor 在绝大多数配置上提升精度；JIT 动态量化因破坏校准一致性而性能崩溃。
- **消融实验**：系统研究了：
  - 预测器输入（r, μ, σ, κ 及其组合）→ 确认仅用 r 最优。
  - 预测器函数（多项式 vs. MLP）→ 多项式更优且参数更少。
  - 范围提取位置（Patch Embedding vs. 中间块）→ 浅层最好。
  - 分块大小 K 与多项式度 d → 推荐 K=32, d=1 或 2。
  - 延迟与计算开销测量（Table 4-6）。
- **客观性**：对比公平，包括了同一基线加 ScalePredictor 前后的直接比较。对个别回归情况（QDrop 下 Swin-Small W2/A3 下降 1.78%）也诚实报告。

## 6. 论文的主要结论与发现
- ScalePredictor 显著提升低比特 PTQ 性能，在 W2/A3 设置下最高提升 **+10.58%**（I&S-ViT + Swin-Small），在 W3/A3 下可达 **+8.71%**（BRECQ + ViT-Small）。
- 推理开销极小：端到端延迟仅比静态 PTQ 增加 **0.6%**，尺度计算开销比 JIT 方法低 **40~1000×**。
- 浅层鲁棒范围与深层最优尺度间存在强相关性，多项式预测器能够有效建模该关系，且比 MLP 更高效、更准确。
- 方法可即插即用到主流 PTQ 框架中，无需修改权重量化模块。

## 7. 优点
- **创新性**：首个将动态量化思想系统引入 ViT PTQ 的工作，基于经验发现的跨层相关性，避免硬件不友好的运行时归约。
- **高效性**：多项式预测器仅需 O(d) 浮点运算，一次范围提取后为所有层共享，几乎不增加推理延迟。
- **普适性**：可无缝集成到 BRECQ、QDrop、AdaLog、I&S-ViT 等多种基线，且在各种 ViT 架构和位宽上一致受益。
- **理论动机**：从泰勒展开角度为多项式形式提供解释，并设计初始化策略保证稳定性。
- **消融完整**：对输入统计量、预测器形式、提取位置、超参数（K, d）等进行了细致分析，结论可靠。

## 8. 不足与局限
- **少量性能下降**：在 QDrop 基线下，Swins (W2/A3) 和部分 W4/A3 配置出现微小退化（最大 -1.78%），作者归因于 ScalePredictor 与 QDrop 的 dropout 校准交互不佳。
- **极低基线场景**：当静态基线准确率已接近随机（如 BRECQ + ViT-Small W2/A3 仅 1.10%），ScalePredictor 提升幅度（+3.05%）虽然相对大，但绝对精度仍很低，实用性有限。
- **模型规模覆盖**：仅测试了 ViT-S/B、DeiT-T/S/B、Swin-S/B，未包含更大模型（如 ViT-L/XL、Swin-L）或最新架构（如 ViT-22B 等），泛化性需进一步验证。
- **任务覆盖**：仅在 ImageNet 分类上评估，未在检测、分割等下游任务上验证。
- **超参数敏感**：分块数 K 和多项式度 d 需手动调节，且 d 在不同架构/位宽下最佳值可能不同（文中建议 K=32, d≤2）。
- **校准集规模固定**：所有实验使用 1024 张图像，未探究更少/更多校准数据对性能的影响。

（完）
