---
title: Towards a Dynamic and Fixed-budget Memory Bank for Efficient Streaming Video Understanding
title_zh: 迈向动态固定预算记忆库的高效流式视频理解
authors: "Baiyang Song, Yuli Lin, Qiong Wu, Tao Chen, Jun Peng, Xiao Chen, Yiyi Zhou, Rongrong Ji"
date: 2026-06-24
pdf: "https://arxiv.org/pdf/2606.25658v1"
tags: ["query:mec-video"]
score: 9.0
evidence: 流式视频理解与动态记忆库
tldr: "流式视频理解中，多模态大模型面临帧数增长和内容不可预测的挑战。本文提出CausalMem，一种无训练的动态固定预算记忆库，通过在线语义基估计视觉令牌冗余并更新记忆，最大化有限空间内的信息量。在LLaVA-OneVision和Qwen2.5-VL上，流式和离线基准分别提升3.2%和3.0%准确率；12k令牌可压缩一小时视频20倍，仅占82MB存储。该方法为高效、紧凑的流式视频语义保留提供了新思路。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.25658v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1386, \"height\": 531, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25658v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1439, \"height\": 581, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25658v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1390, \"height\": 423, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25658v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1369, \"height\": 452, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.25658v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1439, \"height\": 1646, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.25658v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1446, \"height\": 320, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25658v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1442, \"height\": 276, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25658v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1437, \"height\": 678, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25658v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 701, \"height\": 591, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.25658v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 702, \"height\": 587, \"label\": \"Table\"}]"
motivation: 现有流式视频理解方法难以在固定预算下有效管理持续增长的视频帧信息，且无法适应未来内容的不确定性。
method: CausalMem通过在线语义基动态估计视觉令牌冗余，无需训练即可更新固定预算记忆库，最大化信息保留。
result: "流式和离线基准上准确率分别提升3.2%和3.0%，12k令牌预算实现20倍压缩，仅需82MB存储一小时视频。"
conclusion: CausalMem以无训练方式高效压缩流式视频语义，显著优于现有方法，适用于资源受限场景。
---

## 摘要
目前，流式视频理解对于现有的多模态大语言模型（MLLMs）来说仍是一项艰巨的任务。其困难不仅在于处理不断增加的视频帧，还在于未来视频内容和输入指令的不可预测性。在本文中，我们从构建一个动态但固定预算的记忆库的角度研究这一任务，并提出了一种新颖且无需训练的方法，称为CausalMem。CausalMem致力于构建一种动态的视觉记忆更新机制，从而在有限的存储空间中最大化流式视频的信息量，就像人脑一样。在实践中，CausalMem通过在线语义基估计视觉标记的冗余，并更新记忆库，该语义基对观察到的视频流的主要语义进行建模。为了验证CausalMem，我们将其分别应用于两个代表性的MLLMs，即LLaVA-OneVision和Qwen2.5-VL，并在流式和离线视频理解基准上进行了大量实验。实验结果不仅显示了在流式和离线设置下相比现有方法的巨大优势，例如平均准确率分别提高+3.2%和+3.0%，而且见证了流式视频的优越语义保留能力，例如使用12k令牌预算记忆长达一小时的流式视频，实现了超过20倍的视觉令牌压缩比，仅占用约82 MB的存储空间。我们的代码可在CausalMem获取。

## Abstract
Currently, streaming video understanding is still a daunting task for existing \emph{multimodal large language models} (MLLMs). Its difficulties not only lie in handling the ever-increasing video frames, but also in the unpredictability of future video content and input instructions. In this paper, we study this task from the perspective of constructing a dynamic but fixed-budget memory bank, and propose a novel and training-free approach termed \emph{\textbf{CausalMem}}. CausalMem is dedicated to constructing a dynamic visual memory update mechanism, thereby maximizing the amount of information in streaming video within a limited memory space, much like the human brain. In practice, CausalMem estimates the redundancy of visual tokens and updates the memory bank via an online semantic basis, which models the principal semantics of the observed video stream. To validate CausalMem, we apply it to two representative MLLMs, namely LLaVA-OneVision and Qwen2.5-VL respectively, and conduct extensive experiments on both streaming and offline video understanding benchmarks. The experimental results not only show the great advantages than existing methods under both streaming and offline settings, \emph{e.g.}, $+3.2\%$ and $+3.0\%$ average accuracy gains respectively, but also witness the superior semantic preservation for streaming videos, \emph{e.g.}, using 12$k$ token budgets to memorize hour-long streaming videos, which achieves more than \textbf{20$\times$} visual token compression ratio and only occupies about \textbf{82 MB} storage. \textbf{Our code} is given in \href{https://github.com/hktk07/CausalMem}{CausalMem}.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：现有的多模态大语言模型（MLLMs）在流式视频理解场景中面临两大挑战：① 视频帧数持续增长导致视觉令牌数量线性增加，计算和存储负担不可控；② 未来视频内容和用户指令不可预测，传统的基于全局语义或文本引导的压缩方法无法适用。
- **整体含义**：本文旨在构建一个**动态且固定预算**的视觉记忆库，模拟人脑的信息保留机制，在有限存储空间内最大化流式视频的信息量，从而实现高效的流式视频理解。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：提出一种**无需训练**的流式视频理解方法 CausalMem，通过在线语义基（Online Semantic Basis）动态估计视觉令牌的冗余度，并据此更新一个固定预算的记忆库，在严格因果性（无未来信息）下实现信息最大化保留。
- **关键技术细节**：
  - **在线语义基构造与更新**：
    - 初始化：对第一帧视觉令牌进行紧凑奇异值分解（SVD），保留前 \(q_1\) 个右奇异向量作为初始基 \(B_1\)。
    - 逐帧更新：新帧令牌投影到当前基上得到重建残差 \(R_t = X_t - \hat{X}_t\)，残差范数衡量令牌冗余度。选取冗余度最低的 top-k 令牌作为候选，对其残差进行 QR 分解得到新正交基，与旧基拼接后再根据活动分数（结合历史与当前帧的激活程度）保留 top-q 个基向量。
    - 活动分数采用指数移动平均：\(s_{t,j} = \gamma s_{t-1,j} + (1-\gamma)a_{t,j}\)，其中 \(a_{t,j}\) 为当前帧的瞬时激活。
  - **动态视觉记忆库更新**：
    - 每帧到来时，先将该帧所有令牌插入上一时刻的记忆库得到临时记忆库 \(M'_t\)。若超出预算 \(b\)，则基于在线语义基计算每个令牌的冗余度得分（归一化残差）和时域新鲜度得分（帧索引时序），加权融合得到保留分数 \(f_i = (1-\alpha)\tilde{e}_i + \alpha\tilde{\tau}_i\)。
    - 保留分数最高的 \(b\) 个令牌作为更新后的记忆库 \(M_t\)。当用户问题到来时，基于记忆库生成答案。
- **算法流程**：伪代码见原文 Alg.1，核心是每帧编码后更新基和记忆库，最后用记忆库回答。

## 3. 实验设计：数据集/场景、benchmark、对比方法
- **流式理解基准**：
  - **OVO-Bench**：时间戳感知的流式推理（包括 OCR、动作识别、空间理解等6个子任务）。
  - **StreamingBench**：持续长视频理解（包括因果推理、事件理解、时序推理等10个子任务）。均采用实时子集。
- **离线理解基准**（严格按流式因果性评估）：
  - **Video-MME**（数秒至1小时视频）、**LongVideoBench**、**MLVU**（最长约2小时）、**LVBench**（1-2小时极长视频）。
- **对比方法**：
  - 流式方法：ReKV、LiveVLM、StreamMem、StreamingTOM、StreamKV；离线方法（但也在流式设置下对比）：VisionZip（离线）；此外对比了先进Video-MLLMs如 Gemini 1.5 Pro、GPT-4o、LLaVA-OneVision 等。
  - 基线 MLLM：LLaVA-OneVision 和 Qwen2.5-VL。

## 4. 资源与算力
- **明确说明**：所有实验在 **NVIDIA A800 GPU** 上完成。未提及具体数量、训练时长（因为方法无训练，仅推理）。文中报告了推理时间和GPU内存开销（图3），CausalMem在30分钟视频（1FPS）上推理时间约21秒，GPU内存约35 GB，低于其他流式方法（如StreamingTom约25秒/46 GB）。

## 5. 实验数量与充分性
- **实验数量**：包含5个基准（2个流式+3个离线，另加MLVU作为离线代表，共4个离线）上的主要结果，以及与多种SOTA方法的对比（表1-3）。消融实验（表4）覆盖了基向量数 \(q\)、更新令牌数 \(m\)、活动分数组合、保留分数组合等关键因素。此外还包括可视化分析（图4）和效率对比（图3）。
- **充分性与公平性**：
  - 充分：在多个场景、多个基模型、多种对比方法下验证。
  - 公平：所有流式方法均在相同采样率（0.5fps或1fps）和token预算（12k或6k）下比较；CausalMem在离线基准上也按流式因果性评估，无未来信息优势。消融实验逐步验证每个组件贡献。

## 6. 论文的主要结论与发现
- CausalMem在流式基准上平均准确率比基线（LLaVA-OneVision）提升 **3.2%**，在离线基准上提升 **3.0%**，均优于现有流式压缩方法。
- 采用12k token预算即可压缩一小时视频超过**20倍**，仅占用**82MB**存储。
- 在线语义基能够有效建模视频流的主要语义，基于冗余度的保留机制与时域新鲜度结合，实现了信息最大化。
- 效率显著：推理时间和GPU内存均低于对比方法，且随帧数增长扩展性最好。

## 7. 优点：方法或实验设计上的亮点
- **无训练**：不需要额外训练或微调，可直接应用于现有MLLMs，实用性强。
- **固定预算动态更新**：突破传统线性增长限制，在严格因果性下实现固定计算和存储开销。
- **双重分数设计**：冗余度得分保证语义多样性，时域分数保证对近期事件的敏感性，两者互补。
- **在线语义基的数学保证**：维持行正交归一性（见附录B证明），生成语义正交基，避免冗余积累。
- **实验充分且对比全面**：涵盖流式和离线多个基准，与专有流式方法及SOTA通用模型对比，消融实验设计合理。
- **可视化分析**直观展示记忆库如何保留关键语义令牌。

## 8. 不足与局限
- **依赖基模型能力**：CausalMem作为插件方法，其性能受限于所用MLLM的视觉感知、时序推理和指令遵循能力。如果基模型较弱，提升有限（论文在局限部分明确提到）。
- **超参数敏感**：基向量数 \(q\)、更新令牌数 \(m\)、平衡系数 \(\alpha\)、衰减因子 \(\gamma\) 需要根据场景调节，当前实验仅在固定设置下进行，未探讨跨场景调参的鲁棒性。
- **仅针对视觉令牌压缩**：未考虑音频或其他模态压缩，当前仅处理视觉流。
- **实验覆盖**：虽涵盖多个基准，但所有视频均以固定帧率采样（0.5/1fps），未测试更低帧率或动态帧率情景；未在真实流式环境下部署测试延迟抖动。
- **无长视频多样性分析**：仅展示了一小时视频压缩比，未深入分析不同内容类型（如对话、动作、静态背景）下的保留效果差异。

（完）
