---
title: Video-Text Temporal Localization via Multi-Scale Convolution and Dynamic Routing
title_zh: 基于多尺度卷积与动态路由的视频文本时间定位
authors: "Gengtian Shi, Jinze Yu, Chenhao Wu, Shaofei Wang, Eiji Fukuzawa, Junjie Tang, Hiroshi Onoda, Jiang Liu"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05093v2"
tags: ["query:mec-video"]
score: 8.0
evidence: 视频文本时间定位用于流式视频分析
tldr: "视频文本时间定位需对齐自然语言与视频片段，现有方法难以建模分层时间结构与多对多跨模态对应。提出多尺度时间卷积编码器捕获多粒度运动模式，胶囊动态路由迭代优化片段-查询关联，多任务学习联合优化。ActivityNet Captions上Recall@0.5达42.9%，平均IoU 41.1%，超越强Transformer基线且计算高效。表明分层时间建模与结构化语义路由结合可有效提升细粒度视频语言理解。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05093v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1823, \"height\": 547, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05093v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1411, \"height\": 575, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05093v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 546, \"height\": 568, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05093v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 490, \"height\": 662, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05093v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 922, \"height\": 224, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05093v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 998, \"height\": 259, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05093v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1196, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05093v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1206, \"height\": 258, \"label\": \"Table\"}]"
motivation: 现有方法难以建模视频中的分层时间结构和文本与视频间的复杂多对多对应关系，导致定位精度受限。
method: 提出多尺度时间卷积编码器捕获多粒度运动模式，采用胶囊动态路由迭代优化片段-查询关联，通过多任务学习联合优化。
result: "在ActivityNet Captions上Recall@0.5达42.9%，平均IoU 41.1%，超越强Transformer基线且计算效率高。"
conclusion: 融合分层时间建模与结构化语义路由有效提升视频文本时间定位精度，为细粒度视频语言理解提供新范式。
---

## 摘要
视频文本时间定位要求自然语言查询与对应视频片段之间实现精确对齐，这是多模态理解中的一项基础性挑战。我们提出了一种新颖框架，旨在解决现有方法的两个关键局限：对层次化时间结构建模不足，以及无法处理模态间复杂的多对多对应关系。我们的方法引入了一个多尺度时间卷积编码器，能够捕获不同时间粒度下的运动模式——从瞬时帧转换到扩展动作序列。此外，我们提出了一种基于胶囊的动态路由机制，通过结构化的协议更新迭代优化片段与查询的关联，从而能够灵活建模非单调对齐。这些组件通过一个多任务学习目标统一起来，该目标联合优化了时间边界回归、跨模态语义对齐和胶囊多样性。在ActivityNet Captions上的大量实验表明，我们的方法取得了显著改进，实现了42.9%的Recall@0.5和41.1%的平均IoU，在保持计算效率的同时超越了强大的基于Transformer的基线模型。我们的结果验证了将层次化时间建模与结构化语义路由相结合，为细粒度视频语言理解提供了一种有效解决方案。

## Abstract
Video-text temporal localization requires precise alignment between natural language queries and corresponding video segments, a fundamental challenge in multimodal understanding. We present a novel framework that addresses two critical limitations of existing methods: inadequate modeling of hierarchical temporal structure and inability to handle complex many-to-many correspondences between modalities. Our approach introduces a multi-scale temporal convolutional encoder that captures motion patterns across different temporal granularities - from instantaneous frame transitions to extended action sequences. We further propose a capsule-based dynamic routing mechanism that iteratively refines segment-query associations through structured agreement updates, enabling flexible modeling of non-monotonic alignments. These components are unified through a multi-task learning objective that jointly optimizes temporal boundary regression, cross-modal semantic alignment, and capsule diversity. Extensive experiments on ActivityNet Captions demonstrate significant improvements, achieving 42.9% Recall@0.5 and 41.1% mean IoU, surpassing strong transformer-based baselines while maintaining computational efficiency. Our results validate that combining hierarchical temporal modeling with structured semantic routing provides an effective solution for fine-grained video-language understanding.

---

## 论文详细总结（自动生成）

# 论文总结：Video-Text Temporal Localization via Multi-Scale Convolution and Dynamic Routing

## 1. 核心问题与整体含义（研究动机和背景）
- **任务定义**：视频文本时间定位（Temporal Sentence Grounding / Video Moment Retrieval）旨在从无修剪的视频中，找出与自然语言查询精确对应的起止时间片段。
- **现有局限**：
  - 现有方法（如CLIP、BLIP等预训练模型）擅长粗粒度视频-文本匹配，但难以实现细粒度时刻级定位。
  - 两大关键不足：
    1. 缺乏**层次化时间建模**：所有时间尺度被同等对待，无法区分瞬时切换与长程动作。
    2. 无法处理**多对多对应**：实际场景中单个查询可能对应多个不连续片段，或多个查询对应重叠区域，现有注意力机制假设单调或一对一映射，导致对齐模糊。
- **研究动机**：结合层次化时间建模与结构化语义路由，提升细粒度视频语言理解能力。

## 2. 方法论：核心思想、关键技术与流程
- **整体框架**（图2）：特征提取 → 多尺度时间卷积编码 → 胶囊动态路由对齐 → 时间定位头 → 多任务损失。
- **特征提取**：
  - 使用冻结的CLIP编码器（视觉：ViT-B/32等；文本：CLIP Text/BERT等），将帧和词投影到384维共享空间。
  - 加入正弦位置编码注入时间顺序信息。
- **多尺度时间卷积编码器**（图3）：
  - 三个并行1D卷积层，核大小分别为1、3、5，捕获不同粒度：
    - `k=1`：帧级细节与突变
    - `k=3`：短程运动与局部上下文
    - `k=5`：长程动作模式
  - 拼接后经 `1×1` 卷积自适应融合，再加残差连接（保留原始语义）。
  - **复杂度**：`O(T)`，远低于自注意力的 `O(T^2)`。
- **胶囊动态路由对齐模块**（图4）：
  - 将视频帧视为“部分”，文本词视为“整体”，通过迭代路由建立结构化对应。
  - 每个视频特征 `v_i` 经学习变换生成对每个文本词 `q_j` 的“投票” `u_{ij}`。
  - 迭代更新耦合系数 `c_{ij}`（基于与文本词的“协议”，即内积），经 squash 得到聚合表示 `v_j`。
  - 迭代次数 `R=3`。
  - 引入**多样性正则** `L_div`：鼓励不同胶囊表示不同语义，防止退化（公式12）。
- **时间定位头**：
  - 两层的MLP，输入对齐后的表示 `h_t`，输出开始和结束 logits。
  - 解码策略：推荐使用**soft期望**（加权平均）以获得平滑边界。
- **多任务损失**（公式16-19）：
  - `L_loc`：边界回归的BCE损失，对真实边界使用高斯平滑（σ=0.1）提供软监督。
  - `L_align`：InfoNCE对比损失，温度τ=0.07，负样本来自同批次。
  - `L_div`：胶囊多样性正则。
  - 权重：`λ_loc=1.0, λ_align=0.5, λ_div=0.1`，其中 `λ_div` 在训练10个epoch后激活（课程学习策略）。

## 3. 实验设计
- **数据集**：ActivityNet Captions（约20K YouTube视频，100K+人工标注句子），标准划分（37,421训练，17,505验证）。
- **预处理**：1 fps采样，帧大小224×224，过滤长度<2秒或标注不一致的样本。
- **基准与评价**：
  - 指标：Recall@K（IoU阈值0.1/0.3/0.5/0.7）及平均IoU（mIoU）。
  - 对比方法：主要与自身基线（Transformer-only）及添加不同模块后的变体对比，未列举其他SOTA方法（仅称“超越强Transformer基线”）。
- **关键实验**：
  - 不同视觉骨干（ViT-B/32、B/16、L/14）对比（表1）。
  - 不同文本编码器（CLIP-L/14、CLIP-B/16、BERT-base、RoBERTa-base）对比（表2）。
  - **消融实验**（表3、表4）：在ViT-B/32和ViT-B/16上，分别移除/添加多尺度编码器和胶囊模块，验证各自贡献。

## 4. 资源与算力
- **硬件**：单块NVIDIA A100 GPU。
- **训练配置**：
  - PyTorch实现，冻结CLIP骨干，学习部分投影层和所有可训练组件。
  - Batch size = 32，Adam优化器，lr=1e-4，weight decay=1e-4，dropout=0.15。
  - 最多50个epoch，提前停止（基于验证mIoU）。
- **论文未说明**：训练时长（小时）、模型参数量等具体信息。

## 5. 实验数量与充分性
- **实验组数**：
  - 视觉骨干×3，文本编码器×4，消融实验×2（两种骨干下各4种配置，共8组），合计约15组关键对比。
  - 另有解码策略讨论及损失权重说明（未展示详细表格）。
- **充分性评价**：
  - **优点**：消融实验设计清晰，分别验证了两个核心组件的独立与联合效果，且在不同骨干上重复，证明通用性。
  - **不足**：
    - 仅使用单一数据集，缺乏跨数据集（如Charades-STA、TACoS）验证，泛化性存疑。
    - 未与当前SOTA方法（如2D-TAN、VSLNet、VTP、TFVTG等）进行公平量化对比，仅凭“超越强Transformer基线”说服力有限。
    - 基线定义（“Transformer only”）未明确描述其具体结构（可能只是简单线性层或普通Transformer？），使得消融的相对增益难以与其他文献对标。

## 6. 主要结论与发现
- 联合多尺度卷积与胶囊动态路由**显著提升**定位精度：在ViT-B/32上，mIoU从0.328提升至0.386（+17.7%），R@0.5达42.9%（+8.6个百分点）。
- 两者**互补**：联合效果超过各自单独贡献之和（表3、表4）。
- **文本编码器是关键瓶颈**：BERT-base远超CLIP文本及RoBERTa，表明深度语言理解对任务至关重要。
- **软监督策略**（高斯平滑边界标签）有助于处理时间标注的模糊性。
- 方法在保持计算效率（O(T)）的同时取得高性能，适合实际部署。

## 7. 优点
- **架构创新**：
  - 多尺度卷积高效捕获层次化时间模式，复杂度线性。
  - 胶囊动态路由提供结构化、可解释的对齐，克服注意力机制的弥散性。
- **多任务目标**：联合优化边界回归、对比对齐和胶囊多样性，形成协同训练信号。
- **实用性**：冻结视觉/文本骨干，仅训练轻量模块，避免大规模微调，降低计算和存储成本。
- **实验设计**：消融实验详实且跨骨干验证，结果可信。

## 8. 不足与局限
- **实验覆盖不足**：
  - 仅在ActivityNet Captions上评测，未涉及其他主流基准（如Charades-STA、TACoS），存在过拟合风险。
  - 缺少与近期SOTA的全面对比，难以评估在整体领域中的领先程度。
  - 未测试零样本或跨域迁移能力。
- **方法局限性**：
  - 依赖固定帧率（1 fps），可能丢失高帧率场景下的细节。
  - 胶囊路由虽然可解释，但迭代过程可能引入额外延迟，且对超参数（迭代次数、温度等）敏感。
  - 冻结骨干虽高效，但也限制了利用大规模视频预训练模型端到端优化的潜力。
- **报告不透明**：
  - 基线“Transformer only”未定义具体实现，读者无法复现或比较。
  - 未公开代码或训练细节（如数据增强、早停阈值等），可复现性不足。

（完）
