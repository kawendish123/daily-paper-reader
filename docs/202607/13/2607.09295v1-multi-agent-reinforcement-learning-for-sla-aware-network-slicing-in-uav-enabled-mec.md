---
title: Multi-Agent Reinforcement Learning for SLA-Aware Network Slicing in UAV-Enabled MEC
title_zh: 无人机辅助移动边缘计算中面向SLA感知网络切片的多智能体强化学习
authors: "Mohammad Farhoudi, Zeinab Sasan, Masoud Shokrnezhad, Tarik Taleb"
date: 2026-07-10
pdf: "https://arxiv.org/pdf/2607.09295v1"
tags: ["query:mec-video"]
score: 9.0
evidence: 无人机移动边缘计算中面向SLA感知网络切分的多智能体强化学习
tldr: 无人机MEC中异构网络切片面临动态用户移动和资源约束下SLA保障挑战。提出预测性多智能体RL框架，利用轻量用户移动预测和MAPPO协调轨迹控制与资源分配。事件驱动模拟显示SLA稳定性显著优于基线，能耗和延迟接近最优。该框架通过主动预测实现SLA与能效的平衡，提供可扩展的在线决策方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.09295v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 924, \"height\": 571, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09295v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 926, \"height\": 693, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.09295v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1850, \"height\": 765, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.09295v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1881, \"height\": 323, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.09295v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 942, \"height\": 457, \"label\": \"Table\"}]"
motivation: 无人机MEC中动态用户移动和随机任务导致SLA违规，传统方法难以兼顾多切片稳定性与资源效率。
method: 设计预测模块提前感知用户移动，使用MAPPO集中训练分散执行，协调无人机轨迹和计算资源分配，并构建SLA感知奖励函数。
result: 事件驱动模拟在真实移动轨迹下，SLA违规概率和持续时间大幅降低，能耗和延迟接近最优水平。
conclusion: 预测性多智能体RL框架有效提升无人机MEC中多切片SLA稳定性，同时保持能效和延迟竞争力。
---

## 摘要
无人机辅助的移动边缘计算为异构网络切片（包括超可靠低延迟通信、增强型移动宽带和海量机器类通信）提供了灵活的容量配置。然而，在动态用户移动性、随机任务到达以及有限的机载能源和计算资源约束下，保证切片级别的服务等级协议仍然是一个基本挑战。本文提出了一种预测性多智能体强化学习框架，通过协调轨迹控制和计算资源分配，主动维持无人机辅助移动边缘计算中的SLA稳定性。一个轻量级预测模块预测近期的用户移动性，使无人机能够在SLA违规发生前预测拥塞并重新定位。我们设计了一个SLA感知的奖励函数，明确惩罚跨切片的违规概率和持续时间，以及总能耗。无人机智能体使用多智能体近端策略优化进行训练，采用集中训练和分散执行，实现可扩展的在线决策。基于真实移动轨迹的事件驱动模拟表明，与基线相比，所提出的框架显著提高了SLA稳定性，同时保持了具有竞争力的能效和延迟性能，在足够准确的预测信息下接近理想性能。

## Abstract
Unmanned Aerial Vehicle (UAV)-enabled Mobile Edge Computing (MEC) offers flexible capacity provisioning for heterogeneous network slices, including Hyper-Reliable and Low-Latency Communication (HRLLC), Enhanced Mobile Broadband (eMBB), and Massive Machine-Type Communications (mMTC). However, guaranteeing slice-level Service-Level Agreements (SLAs) under dynamic user mobility, stochastic task arrivals, and constrained onboard energy and computing resources remains a fundamental challenge. This paper proposes a predictive multi-agent Reinforcement Learning (RL) framework that proactively maintains SLA stability in UAV-enabled MEC through coordinated trajectory control and computation resource allocation. A lightweight prediction module forecasts near-future user mobility, enabling UAVs to anticipate congestion and reposition before SLA violations occur. We design an SLA-aware reward function that explicitly penalizes both violation probability and duration across slices, alongside total energy consumption. UAV agents are trained using Multi-Agent Proximal Policy Optimization (MAPPO) with centralized training and decentralized execution, enabling scalable online decision-making. Event-driven simulations with realistic mobility traces demonstrate that the proposed framework significantly improves SLA stability compared with baselines while maintaining competitive energy efficiency and delay performance, approaching oracle-level performance with sufficiently accurate predictive information.

---

## 论文详细总结（自动生成）

# 论文结构化总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：无人机辅助移动边缘计算（UAV-MEC）集成了网络切片技术，可为超可靠低延迟通信（HRLLC）、增强型移动宽带（eMBB）和海量机器类通信（mMTC）等异构服务提供灵活的边缘计算能力。然而，在实际动态环境中，用户移动性随机、任务到达不可预测、无人机能源和计算资源有限，使得保证各切片的服务等级协议（SLA）变得极其困难。现有工作大多优化传统QoS（如延迟、吞吐量）而非SLA，且多采用反应式控制，缺乏主动预防机制。
- **整体含义**：该论文致力于解决无人机MEC系统中多切片SLA稳定性的核心挑战，提出一种基于预测的多智能体强化学习框架，旨在通过主动轨迹控制和资源分配，在动态条件下维持SLA的稳定满足，同时兼顾能效。

## 2. 方法论：核心思想、关键技术细节

### 核心思想
- **主动预防**：利用轻量级预测模块估计用户未来位置和任务生成概率，使无人机能提前预判潜在拥塞和SLA违规，从而主动调整轨迹和计算资源。
- **多智能体协作**：每架无人机作为独立智能体，在集中训练-分散执行（CTDE）范式下，通过MAPPO协同优化。
- **SLA感知奖励**：奖励函数明确惩罚瞬时违规概率、持续违规（超过阈值）以及预测的未来违规，同时考虑能耗，实现多目标权衡。

### 关键技术细节
1. **预测模块**：
   - 使用D3QL（Dueling Double Deep Q-learning）架构，结合LSTM捕获时间依赖、CNN提取局部模式，最终通过全连接层输出Q值。
   - 预测下一时刻用户位置 \(\hat{w}_k(t+1)\) 和任务生成概率 \(\hat{p}_{\lambda_k}(t+1)\)。
   - 奖励函数根据预测与真实值匹配情况（位置+任务生成）赋予权重 \(\omega_\alpha\) 和 \(\omega_\lambda\)。

2. **决策模块（MAPPO）**：
   - **观测空间**：每架无人机 \(u\) 的局部观测 \(o_u(t)\) 包括当前位置、剩余能量、当前计算负载、用户信息（任务参数、位置、预测位置和生成概率）等。
   - **动作空间**：无人机位移 \(\Delta q_u(t)\) 和分配给每个可观测用户的CPU资源 \(f_{k,u}(t)\)，受限于最大速度与计算容量。
   - **用户关联规则**：基于最大传输速率选择无人机：\(a_{k,u}(t)=1\) if \(u = \arg\max_j R_{k,j}(t)\)。
   - **共享奖励**：\(r(t) = -\big[ \sum_s b_s P^{\text{viol}}_s(t) + \sum_s \beta_s \mathbb{I}(P^{\text{viol}}_s(t)>\epsilon_s) + \sum_s b_s \hat{P}^{\text{viol}}_s(t+1) + \chi \sum_u E_u(t) \big]\)，其中 \(\hat{P}^{\text{viol}}_s(t+1)\) 基于预测信息计算。
   - **策略更新**：使用GAE计算优势函数，PPO裁剪目标函数更新策略，防止更新过大。

3. **问题形式化**：原问题为非凸、随机、时间耦合的联合优化问题，目标是最小化SLA违规概率、违规持续时间和总能耗，受限于轨迹约束、关联约束、计算容量约束和能量约束。

## 3. 实验设计：数据集/场景、Benchmark、对比方法

- **场景与参数**：
  - 区域：1000×1000 m²，3架UAV，24个用户（默认），用户任务分为HRLLC、eMBB、mMTC三类（参数见表II）。
  - 无人机：速度30 m/s，CPU容量12 GHz，能量预算5×10⁷ J。
  - 用户轨迹：使用YJMob100K真实移动性数据集。
  - 时间离散化，每个时隙处理任务。
  - 预测模块结构：LSTM 128单元，卷积核(3,2,2)，学习率10⁻⁴，折扣因子0.99，GAE λ=0.95，PPO裁剪0.2，熵系数0.02；训练1500个episode，评估10个episode。

- **Benchmark与对比方法**：
  - **GA-Search**：基于遗传搜索的轨迹和资源分配（离散化搜索）。
  - **Greedy**：根据计算延迟紧迫度优先服务用户。
  - **Random**：随机选择和资源分配。
  - **Offline-Optimal / Informed-MAPPO**：作为Oracle上界，使用完整未来信息（预测场景中为Informed-MAPPO，即拥有真实未来移动和任务信息）。
  - 所有非Oracle方法均使用相同的预测信息，以公平评估决策算法本身。

- **两个主要实验场景**：
  1. **改变用户数量**（从少到多，模拟用户高峰）：对比MAPPO与GA-Search、Greedy、Random、Oracle，评估延迟、SLA违规概率、能耗。
  2. **改变UAV数量**（从少到多，模拟资源充足性）：比较Predictive-MAPPO（使用预测）与Informed-MAPPO（使用真实未来信息），评估相同指标，检验预测模块的效能。

## 4. 资源与算力

- **文中未明确说明**：未提及使用的GPU型号、数量、训练时长。仅提到“事件驱动模拟”（event-driven simulations），训练1500个episode，评估10个episode，每个episode对应一个固定场景。未提供实际运行时间或硬件细节。

## 5. 实验数量与充分性

- **实验数量**：共两组主要实验（用户数变化、UAV数变化），每组包含三个指标（延迟、SLA违规概率、能耗），每个指标绘制折线图展示趋势。未展示具体的数值表格或置信区间。
- **充分性与客观性**：
  - 优点：对比了多种基线（包括Oracle），覆盖了不同压力条件下的性能；使用真实移动轨迹（YJMob100K），场景较真实。
  - 不足：
    - **缺少消融实验**：未单独剥离预测模块、SLA奖励项、能量项等组件对性能的贡献。
    - **规模有限**：仅24用户、3架UAV，未验证更大规模（如50+用户、10+UAV）下的可扩展性。
    - **未量化预测精度影响**：虽然对比了Predictive与Informed，但未分析不同预测精度（如加入噪声）对结果的影响。
    - **无统计重复性说明**：未说明多次实验的随机种子或置信区间，图表看起来为单次或平均结果。
    - **指标完整性**：虽包含SLA违规概率和持续时间，但未展示各切片（HRLLC、eMBB、mMTC）的单独表现，仅整体SLA违规率。

总体而言，实验设计基本合理，验证了核心思想，但在深度和广度上还有提升空间。

## 6. 主要结论与发现

1. **SLA稳定性显著提升**：所提出的Predictive-MAPPO在SLA违规概率和违规持续时间上显著优于GA-Search、Greedy、Random等非理想方法，在所有用户密度和UAV数量下均接近Oracle性能。
2. **能效与延迟竞争力**：能耗与延迟表现与大多数基线相当或更优，表明SLA稳定性的提升并非以过度能耗为代价，而是通过智能协调避免无效移动和过分配置。
3. **预测模块有效性**：当UAV数量充足时，Predictive-MAPPO与使用真实未来信息的Informed-MAPPO性能差距极小，说明轻量级预测模块提供了足够准确的未来信息用于主动控制。
4. **资源紧张时预测更关键**：在UAV数量较少（资源短缺）时，预测误差影响更大，但Predictive-MAPPO仍能显著优于无预测的其他方法，验证了主动控制的价值。

## 7. 优点

- **问题新颖性与完整性**：首次将SLA稳定性（包括违规概率和持续时间）显式建模并作为优化目标，而非仅关注传统QoS。
- **主动预防机制**：通过预测用户移动和任务生成，实现提前决策，避免反应式控制的滞后性。
- **多智能体框架设计合理**：采用MAPPO+CTDE，适合分布式部署且能利用全局信息训练，奖励函数联合考虑多切片、能耗和预测，设计精巧。
- **轻量预测模块**：D3QL结合LSTM+CNN，在保持预测精度的同时计算开销可控。
- **实验对比全面**：包含Oracle上界和多种启发式方法，能清晰展示不同策略在SLA稳定性、延迟、能耗方面的权衡。

## 8. 不足与局限

- **实验规模与泛化性有限**：仅24用户和3架UAV，未涉及更大规模或更复杂地形（如多障碍物、三维空间遮挡）。无法直接判断在真实大规模部署中的可扩展性。
- **缺少消融研究**：未系统分析预测模块、SLA惩罚项、能量惩罚项的各自贡献，不利于理解各组件的重要性。
- **预测精度分析不足**：虽然对比了Informed-MAPPO，但未在预测模块不同程度的误差下进行鲁棒性测试。
- **未考虑更真实的物理模型**：未包含推进能量精确模型、多无人机干扰、上行功率优化、信道动态性等，简化了通信和能量模型，可能影响实际效果。
- **未涉及实时性分析**：未评估预测和决策模块的计算延迟是否满足决策时隙要求（论文假设每个时隙内完全决策）。
- **未讨论动态切片准入与带宽分配**：仅固定三种切片类型，未考虑切片动态加入或退出，以及频谱资源的自适应分配。
- **缺少不确定性量化**：预测模块为点估计（位置和概率），未输出置信度，可能影响高风险场景下的决策可靠性。

（完）
