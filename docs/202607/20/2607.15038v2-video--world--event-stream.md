---
title: Video = World + Event Stream
title_zh: 视频 = 世界 + 事件流
authors: "Lianghua Huang, Zhi-Fan Wu, Yupeng Shi, Wei Wang, Mengyang Feng, Cheng Yu, Chen Liang, Junjie He, Chen-Wei Xie, Yu Liu, Jingren Zhou, Ang Wang, Bang Zhang, Baole Ai, Chongyang Zhong, Jinwei Qi, Kai Zhu, Pandeng Li, Peng Zhang, Wenyuan Zhang, Xinhua Cheng, Yitong Huang, Yun Zheng, Yuxiang Bao, Yuzheng Wang, Zhiwei Lin, Zoubin Bi"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15038v2"
tags: ["query:mec-video"]
score: 8.0
evidence: 面向实时视频预测与交互的通用预训练任务
tldr: 将视频建模为世界（稳定上下文）与事件流（动态变化）的组合，提出通用预训练任务：给定世界和输入，预测世界实时变化。实例化于实时全双工视听交互，模型采用视觉-语言-动作映射。Wan-Streamer v0.3实现640x368像素25FPS流式处理，模型响应延迟约200ms，总交互延迟约550ms。该框架为实时视频交互提供统一范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15038v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1721, \"height\": 673, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15038v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1720, \"height\": 776, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15038v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1709, \"height\": 686, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15038v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1721, \"height\": 665, \"label\": \"Table\"}]"
motivation: 现有视频模型难以统一建模稳定上下文与动态变化，导致实时交互任务中延迟高、理解不连贯。
method: 提出World+Event Stream分解框架，设计预测世界变化的预训练任务，并在全双工视听交互中采用多模态映射实现实时响应。
result: Wan-Streamer v0.3在实时全双工视听交互中实现约200ms模型响应延迟和约550ms总交互延迟。
conclusion: 该框架为实时流式视频交互提供通用范式，有效平衡上下文保持与动态响应，可扩展至多种实时下游任务。
---

## 摘要
我们提出 Wan-Streamer v0.3，该版本在统一组织视角下重新构建了原生流式交互模型：视频是一个世界加上一个事件流。世界是视频展开的持久上下文，包括环境、场景、主体、环境声学条件、语音特征以及其他相对稳定的条件。事件流是在该世界中随时间变化的一切，包括场景或环境变化、主体行为、语音和其他声音。这产生了一个基于大量真实视频的通用预训练任务：给定一个世界和传入的输入，预测世界如何实时移动、变化和响应。由此获得的能力可以适用于广泛的实时下游任务系列。我们将其实例化到实时全双工视听交互中，其中事件流是智能体的语音连同自由形式的行为。在功能上，模型的多模态理解过程类似于视觉-语言-动作：它将多模态用户输入映射为语言形式的语音和行为动作。Wan-Streamer v0.3 保持了 v0.2 的操作点：640x368 分辨率视频，25 FPS，160 毫秒流式单元，约 200 毫秒模型端响应延迟，以及在 350 毫秒双向网络预算下约 550 毫秒的总交互延迟。

## Abstract
We present Wan-Streamer v0.3, which reframes our native-streaming interaction model under a single organizing view: a video is a world plus an event stream. The world is the persistent context in which a video unfolds, including the environment, scene, subjects, ambient acoustic conditions, voice characteristics, and other relatively stable conditions. The event stream is everything that changes over time within that world, including scene or environmental changes, subject behavior, speech, and other sounds. This yields a general-purpose pretraining task over large amounts of real video: given a world and incoming input, predict how the world moves, changes, and responds in real time. The resulting competence can be specialized to a broad family of real-time downstream tasks. We instantiate it on real-time full-duplex audio-visual interaction, where the event stream is the agent's speech together with free-form behavior. Functionally, the model's multimodal understanding process is vision-language-action-like: it maps multimodal user input to language-form speech and behavior actions. Wan-Streamer v0.3 preserves the v0.2 operating point: 640x368 video at 25 FPS, a 160 ms streaming unit, approximately 200 ms model-side response latency, and approximately 550 ms total interaction latency under a 350 ms bidirectional network budget.

---

## 论文详细总结（自动生成）

# 论文总结：Video = World + Event Stream (Wan-Streamer v0.3)

## 1. 核心问题与整体含义（研究动机和背景）
- **研究动机**：现有实时全双工视听交互模型（如Wan-Streamer v0.1/v0.2）是针对单一交互任务设计的，缺乏统一的底层能力框架。作者希望提出一个更通用的先验任务，使得模型能够从大量视频中学习“世界如何随时间演变”的常识，并通过微调适配多种实时下游任务（如交互、漫游、具身操作等）。
- **核心问题**：如何将视频分解为稳定上下文（世界）与动态变化（事件流），并以此设计预训练目标，使模型具备预测世界实时演化的能力。
- **整体含义**：视频 = 世界 + 事件流。世界是持久的环境、场景、角色等；事件流是随时间发生的一切变化。这种分解允许统一的预训练和迁移，并在全双工视听交互中实现低延迟、高表现力的实时行为生成。

## 2. 提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：将视频建模为两个部分的组合：**世界**（持久上下文，包括环境、场景、角色身份、环境声等）和**事件流**（时间局部变化，如行为、对话、环境变化等）。预训练任务定义为：给定世界和输入，预测世界如何实时变化（事件流）。
- **关键技术细节**：
  - **世界表示**：结构化记录 `W = {w_j}`，包含视觉上下文、声学上下文、角色记录等，模式不固定。
  - **事件表示**：`e_k = (τ_k, c_k, d_k)`，其中τ_k为时间区间，c_k为关联角色（或独立事件），d_k为自由描述（行为、语音等）。
  - **流式分解**：模型按因果顺序预测事件序列，公式：
    ```
    pθ(e_1:K | W, x_1:K) = ∏_{k=1}^K pθ(e_k | W, x_≤k, e_<k)
    ```
    其中x_≤k是驱动流的外部输入（如用户观测）。离散部分用next-token预测，连续部分用条件流匹配生成。
  - **预训练**：对大量真实视频提取世界摘要和时间对齐的事件描述（如图1示例），模型学习预测下一事件及其音视频实现。
  - **下游实例化（实时全双工视听交互）**：世界包含场景、角色、环境音、音色；事件流为智能体响应（包含自由行为指令和语音），采用角色扮演格式（括号内行为指令）。多模态理解类似VLA（视觉-语言-动作），将用户输入映射为语言形式的行为和语音，再通过因果解码生成同步的音视频。
  - **系统参数**：保持v0.2配置：640×368@25fps，160ms流式单元，模型端延迟~200ms，总延迟~550ms（350ms网络预算）。

## 3. 实验设计：数据集、基准、对比方法
- **数据集**：论文未明确说明预训练使用哪些具体视频数据集。仅提到“大量普通视频”（large-scale, diverse video corpora），未提供名称和规模。
- **基准**：没有设置标准benchmark。实验仅报告了延迟数据和定性行为观察。
- **对比方法**：未与其他现有方法（如其他实时交互模型）进行定量对比。仅在版本间对比了配置和功能（表1），但无客观性能指标（如MOS、用户评价）。

## 4. 资源与算力
- **未明确说明**：论文未提及训练使用的GPU型号、数量、时长、成本等任何算力信息。仅提到v0.2使用了Ulysses上下文并行，但v0.3继承此拓扑，无具体资源配置。

## 5. 实验数量与充分性
- **实验数量**：极少。仅有延迟测量（保证与v0.2一致）和定性观察示例（生成对话中表现了开放式行为）。
- **充分性**：严重不足。缺乏以下关键实验：
  - 消融实验（如是否预训练、世界表示方式、自由行为格式的影响）
  - 定量评价（如语音自然度、动作合理性、实时交互成功率、延迟稳定性）
  - 多下游任务（仅演示了交互任务，未评估漫游、操作等）
  - 用户调研或对比基线
- **客观性与公平性**：无法评估。缺乏可重复实验细节和对比方法。

## 6. 主要结论与发现
1）视频可统一表示为“世界+事件流”，该分解支持通用预训练和多种实时下游任务的迁移。
2）Wan-Streamer v0.3实例化于全双工视听交互，模型端延迟约200ms，总延迟约550ms，与v0.2持平。
3）自由形式行为（通过角色扮演格式）被成功集成，智能体可执行开放式指令（如“捡起叶子”等），且行为与语音同步。
4）模型采用“视觉-语言-动作”映射，输入多模态用户信号，输出语言形式的语音和行为动作。

## 7. 优点：方法或实验设计上的亮点
- **概念简洁且通用**：World+Event Stream的分解抓住了视频的本质，提供了一种统一预训练视角，可应用于漫游、交互、具身等多个领域，具有较强扩展性。
- **保持低延迟**：在不牺牲v0.2延迟和分辨率的前提下，引入了自由形式行为，说明模型设计高效，无新增延迟关键路径。
- **行为表达灵活**：使用角色扮演格式，将行为指令与语音共用一个token流，学习时机与协同性，避免了外部调度器，具有较好的可扩展性。

## 8. 不足与局限：实验覆盖、偏差风险、应用限制
- **实验覆盖严重不足**：缺乏定量评价、消融研究、对比基线、用户调研，无法验证模型在真实场景中的有效性和可靠性。
- **没有公开预训练细节**：未说明视频数据来源、规模、清洗方式，结果可重复性差。
- **应用限制**：当前仅实现了全双工视听交互一个下游任务，其他声称支持的场景（漫游、操作）未给出任何实验证据。
- **潜在偏差风险**：预训练视频可能来自网络，世界和事件描述依赖标注或自动提取，质量未知，可能导致生成行为与真实世界不完全对齐。
- **缺乏算力和训练成本信息**：不利于评估模型实际可行性。
- **定性结果主观**：论文仅用文本描述生成示例，未提供真实视频或大规模评测，难以判断实际表现。
- **网络预算假设**：总延迟包含350ms双向网络预算，实际用户体验可能受网络波动影响，模型端优化在可控环境测试，真实部署有待验证。

（完）
