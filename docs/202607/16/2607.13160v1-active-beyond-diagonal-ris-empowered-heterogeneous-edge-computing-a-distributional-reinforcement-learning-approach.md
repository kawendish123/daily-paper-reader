---
title: "Active Beyond-Diagonal RIS Empowered Heterogeneous Edge Computing: A Distributional Reinforcement Learning Approach"
title_zh: 超越对角有源可重构智能表面赋能异构边缘计算：一种分布强化学习方法
authors: "Tianyu Pang, Hongyu Li"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.13160v1"
tags: ["query:mec-video"]
score: 8.0
evidence: 主动BD-RIS赋能的异构移动边缘计算
tldr: "主动超越对角线可重构智能表面(BD-RIS)用于异构移动边缘计算(MEC)系统，其互易设备产生跨扇区能量泄漏，影响能效时延折中。本文提出基于分布软演员-评论家(DSAC-T)的端到端联合优化框架，通过建模回报分布而非期望提升策略稳定性。DSAC-T实现81.67%最高可行性比和0.0267秒在线决策，取得最佳能效时延奖励，有效解决高维混合整数非凸问题。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13160v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 872, \"height\": 552, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13160v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1751, \"height\": 937, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13160v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 831, \"height\": 562, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13160v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 912, \"height\": 366, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13160v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 850, \"height\": 265, \"label\": \"Table\"}]"
motivation: 解决主动BD-RIS辅助异构MEC中卸载决策、计算分配、功率与RIS耦合的高维混合整数非凸问题，并应对能量泄漏导致的能效时延折中。
method: 采用分布性强化学习DSAC-T算法，建模回报分布以增强策略稳定性和边界敏感性。
result: "DSAC-T达到81.67%可行性比，在线决策仅0.0267秒，能效时延奖励优于基线。"
conclusion: DSAC-T框架高效优化主动BD-RIS异构MEC，实现最优能效时延性能。
---

## 摘要
有源超越对角可重构智能表面（BD-RIS）支持混合发射与反射模式，实现有效信号放大和全空间覆盖，从而为异构移动边缘计算（MEC）系统中具有遮挡感知的上行卸载提供了一种有前景的解决方案。然而，实际混合模式有源BD-RIS由互易器件实现，这固有地产生跨扇区能量泄漏，将重塑系统级能量-延迟权衡。本文研究了互易有源BD-RIS辅助异构MEC的能量感知卸载与资源分配，其中卸载决策、CPU/GPU计算分配、发射功率、接收处理和有源BD-RIS紧密耦合。由此产生的问题是一个高维混合整数非凸问题，难以通过传统的逐实例优化高效求解。为应对这一挑战，我们开发了一种基于精细化分布软演员-评论家算法（称为DSAC-T）的端到端联合优化框架。通过建模回报分布而不仅仅是期望值，DSAC-T提高了在回报异质性和可行性边界敏感性下的策略稳定性。与其他基线算法相比，DSAC-T实现了最佳的能量-延迟回报，最高可行性比率为81.67%，并且每场景在线决策时间快至0.0267秒。

## Abstract
Active beyond-diagonal reconfigurable intelligent surfaces (BD-RISs) enables hybrid transmitting and reflecting mode to achieve effective signal amplification and full-space coverage, thus providing a promising solution for blockage-aware uplink offloading in heterogeneous mobile edge computing (MEC) systems. However, practical hybrid mode active BD-RIS are realized by reciprocal devices, which inherently generate cross-sector energy leakage that will reshape the system-level energy-latency tradeoff. This paper studies energy-aware offloading and resource allocation for reciprocal active BD-RIS-assisted heterogeneous MEC, where offloading decisions, CPU/GPU computation allocation, transmit powers, receive processing, and active BD-RIS are tightly coupled. The resulting problem is a high-dimensional mixed integer nonconvex problem and is difficult to solve efficiently by conventional per-instance optimization. To address this challenge, we develop an end-to-end joint optimization framework based on a refined version of the distributional soft actor--critic algorithm, named as DSAC-T. By modeling return distributions rather than only expected values, DSAC-T improves policy stability under reward heterogeneity and feasibility-boundary sensitivity. Compared with other baseline algorithms, DSAC-T achieves the best energy-latency reward, the highest feasibility ratio of 81.67%, and a fast online decision time of 0.0267 s per scenario.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究问题**：在异构移动边缘计算（MEC）系统中，如何高效地进行任务卸载与资源分配，使得系统能效与延迟性能最优，且能应对遮挡、跨扇区能量泄漏以及 CPU/GPU 任务异质性带来的挑战。
- **背景**：
  - 传统可重构智能表面（RIS）存在乘法衰落瓶颈，被动式 RIS 性能受限；主动 RIS（有源）能放大信号，但现有模型未完全考虑互易器件带来的跨扇区能量泄漏。
  - 超越对角 RIS（BD-RIS）支持混合发射/反射模式，可实现全空间覆盖，但其物理实现（基于互易电路）会产生跨扇区能量泄漏，压缩可用能量预算，影响能效与延迟折衷。
  - 异构 MEC 中 CPU 和 GPU 任务共存，需要解耦的边缘计算资源池；现有工作大多采用通用计算抽象，未区分 CPU/GPU 资源。
- **核心挑战**：卸载决策、计算分配、发射功率、接收处理、有源 BD-RIS 配置高度耦合，形成高维混合整数非凸问题，传统逐实例优化难以高效求解，而期望值型深度强化学习方法（如 SAC、TD3）在回报异质性和可行性边界敏感场景下性能不稳定。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：将问题建模为槽位学习（slot-wise）马尔可夫决策过程（MDP），采用分布强化学习算法 DSAC-T 进行端到端联合优化。通过建模回报的**分布**（而非仅期望值），提高策略在异质回报和可行性边界附近的稳定性。
- **关键技术细节**：
  - **MDP 设计**：
    - 状态 \(s_t\)：包含任务特征（数据大小、计算强度、截止时间等）、信道信息、本地/边缘资源条件。
    - 动作 \(a_t\)：连续化参数，包括卸载决策（通过映射转为二进制）、边缘计算分配、上行发射功率、有源 BD-RIS 控制向量（\(\vartheta_t\)）。经过环境侧映射模块处理后生效（如功率归一化、RIS 约束投影、MMSE 接收机计算）。
    - 奖励 \(r_t\)：惩罚型代理目标，包含归一化的加权能量消耗（本地、卸载、RIS）和延迟违反惩罚项 \(-\lambda_T V_{T,t}\)，其中 \(V_{T,t}\) 为平均相对延迟违规。
  - **DSAC-T 算法**：
    - 使用**双高斯价值分布** \(Z_{\theta_i}(s,a) \sim \mathcal{N}(Q_{\theta_i}(s,a), \sigma^2_{\theta_i}(s,a))\) 同时学习均值与不确定性。
    - 采用**较小目标均值** \(Q_{\theta_{\min}} = \min_i Q_{\theta_i}\) 构建目标，减少过估计偏差。
    - 更新步骤：
      1. 采样：智能体与环境交互，存储转移至回放缓冲区。
      2. 更新：从缓冲区采样，目标动作由目标策略 \(\pi_{\phi'}\) 采样，计算目标自洽算子 \(T_{\phi',i}\)；通过最小化分布距离损失更新双价值分布；更新策略（最小化 \(\alpha \log \pi - Q_{\theta_{\min}}\)）；更新温度系数 \(\alpha\)；软更新目标网络。
  - **物理建模亮点**：
    - 主动 BD-RIS 采用混合模式，散射矩阵满足互易性（\(\Theta=\Theta^T\)）且子块对角，准确建模跨扇区能量泄漏和放大器效率。
    - 用户分为非遮挡（扇区1）和深度遮挡（扇区2），直接信道中遮挡用户引入严重衰减因子（0.05）而非假设为零。
    - 边缘计算资源解耦为 CPU 和 GPU 两个独立池，分别约束。

## 3. 实验设计

- **场景参数**：
  - 用户数 \(K=10\)（其中4个深度遮挡用户），BS 天线数 \(N=8\)，有源 BD-RIS 单元数 \(M=128\)，带宽 \(B=16\) MHz。
  - 信道：距离相关莱斯衰落；用户-BS距离20-120m，用户-RIS距离5-20m，RIS-BS距离5-20m；路径损耗指数分别为3.5、2.2、2.2；莱斯因子2dB、6dB、10dB。
  - 发射功率范围0.01-0.1W；RIS静态功率0.5W，放大器效率0.4，输出功率预算0.01W。
  - 本地CPU最大频率1.3 GHz，边缘CPU池24 GHz，GPU池48 GHz。
  - 任务：5个CPU型（数据大小8e4-1.8e5 bits，计算强度400-650 cycles/bit，截止时间0.35-0.8s）、5个GPU型（数据大小1.98e5-4.5e5 bits，计算强度900-1300 cycles/bit，截止时间0.198-0.462s）。
  - 奖励权重：\(w_{loc}=0.5, w_{off}=2.0, w_{RIS}=0.5, \lambda_T=1.0\)。
- **Baselines**：
  - 其他深度强化学习方法：DDPG、SAC、TD3。
  - 数学优化方法：基于交替优化与逐次凸近似（AO–SCA）。
- **评估指标**：
  - 平均奖励（越大越好，因定义为负能量-延迟惩罚）。
  - 可行性比率（满足所有约束的场景比例）。
  - 在线决策时间（每场景秒数）。

## 4. 资源与算力

- 论文**未明确说明**所使用的 GPU 型号、数量以及训练时长。仅提及“训练时间不包括在报告结果中”（见表 I 脚注），且在线决策时间仅 0.0267秒/场景。
- 可以推测实验采用常规深度学习硬件（如单 GPU），但具体配置未提供。

## 5. 实验数量与充分性

- **实验组数**：
  - 训练收敛曲线（图3）：比较 DDPG、SAC、TD3、DSAC-T 四条曲线。
  - 性能对比表（表I）：报告4种DRL方法及AO–SCA在300个评估场景上的平均奖励、可行性比率、在线决策时间。
  - 消融实验（图4）：改变有源BD-RIS单元数（M=32,64,96,128），对比DSAC-T学到的RIS控制与随机RIS控制的奖励和可行性比率。
- **充分性评价**：
  - 实验较为充分：对比了多种主流DRL基线（DDPG, SAC, TD3）以及一种数学优化方法（AO–SCA），并设计了单元数的消融实验。
  - 但缺少以下内容：① 对其他关键参数（如遮挡程度、任务异质性比例、功率预算等）的灵敏度分析；② 不同随机种子下的方差统计（文中未报告置信区间）；③ 训练过程中的样本效率或计算开销对比。
  - 总体客观公平，但可进一步丰富以增强泛化性。

## 6. 主要结论与发现

- **DSAC-T 在性能上全面超越基线**：
  - 获得最高平均奖励（-2.828），比 DDPG（-5.899）提升约52%；比 SAC（-4.068）提升约30%。
  - 最高可行性比率 81.67%，比 SAC（71.33%）高10.34个百分点，比 TD3（70.67%）高11.00个百分点。
  - 在线决策时间仅0.0267秒，远快于 AO–SCA（41.865秒），提升超过三个数量级。
- **分布价值学习的关键作用**：DSAC-T 通过建模回报分布，有效缓解了回报异质性和可行性边界敏感带来的更新不稳定问题，使训练收敛更快且最终性能更优。
- **有源 BD-RIS 单元数的影响**：增加 RIS 单元数可同时提升奖励和可行性；且学到的 RIS 控制显著优于随机 RIS 控制，证明显式优化 RIS 系数是必要的。
- **物理建模的合理性**：互易电路模型下的跨扇区能量泄漏被准确捕获，并显式体现在能量约束中，使得优化问题更贴近实际。

## 7. 优点

- **物理模型精细化**：首次将互易有源 BD-RIS 的跨扇区能量泄漏、放大器效率、残余直接链路衰减、解耦 CPU/GPU 资源等实际因素纳入系统模型，增强了问题的真实性和挑战性。
- **算法创新**：针对问题特殊困难（高维混合整数、强耦合、异质回报），选择并改进了 DSAC-T 算法，引入双高斯分布价值学习和较小目标均值，有效提升策略稳定性与可行性。
- **端到端框架**：将原本复杂的迭代优化问题转化为端到端学习，支持实时在线决策（0.0267秒/场景），实用性强。
- **实验对比全面**：与多种 DR L 基线及数学优化方法对比，并从奖励、可行性、计算时延三个维度评估，证明了 DSAC-T 的性能-复杂度折衷优势。
- **消融实验设计合理**：通过改变 RIS 单元数并对比随机控制，验证了 RIS 优化的必要性。

## 8. 不足与局限

- **实验覆盖不够全面**：缺少对不同用户数、不同遮挡比例、不同任务异质性程度、不同功率预算等参数的广泛灵敏度分析；也未报告多次运行下的统计误差（如标准差），结果稳健性难以评估。
- **物理假设可进一步细化**：假设所有用户均为单天线，且 RIS 采用单元级单连接架构；更复杂的组连接架构或多天线用户未考虑。
- **训练成本未报告**：未给出训练所需的 GPU 型号、训练轮数、时间等资源消耗，不利于复现和实际部署评估。
- **基线方法调优程度未知**：基线 DRL 方法（DDPG、SAC、TD3）的超参数是否经过仔细调优未说明，可能存在不公平比较的风险（尽管 DSAC-T 也基于 SAC 改进）。
- **应用限制**：在线决策时间虽快，但训练过程可能仍需大量样本（场景交互），对于快速变化的动态环境，迁移学习或在线持续学习能力未探讨。
- **无公开代码或数据集**：论文未提供代码或数据，可复现性较低。

（完）
