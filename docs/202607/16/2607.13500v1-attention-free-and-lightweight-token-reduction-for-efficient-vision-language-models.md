---
title: Attention-Free and Lightweight Token Reduction for Efficient Vision-Language Models
title_zh: 无注意力且轻量化的令牌缩减以实现高效视觉-语言模型
authors: "Xuanyi Hao, Zuoyuan Zhang, Zhibo Wang, Xiaoyi Pang, Jiahui Hu, Jiacheng Du, Shuguo Zhuo"
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13500v1"
tags: ["query:mec-video"]
score: 9.0
evidence: 面向边缘设备的令牌缩减
tldr: 视觉-语言模型因处理大量视觉token导致计算开销大，难以部署在边缘设备。现有token缩减方法依赖注意力图或高计算量的相似比较，实用性受限。本文提出无注意力的轻量token缩减框架，通过基于熵的重要性估计保留高信息token，并利用变换一致性信号实现低成本的多样性筛选。在多个基准上证明该方法在激进压缩下仍能维持良好性能，实现精度与效率的有利平衡。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13500v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1830, \"height\": 1116, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 800, \"height\": 827, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 889, \"height\": 575, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 932, \"height\": 522, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13500v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 896, \"height\": 587, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13500v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1883, \"height\": 1169, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13500v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 938, \"height\": 205, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13500v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 894, \"height\": 164, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13500v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 926, \"height\": 573, \"label\": \"Table\"}]"
motivation: 现有token缩减方法无法同时兼容加速框架并避免高计算开销，需设计高效且实用的无注意力方案。
method: 采用熵准则估计token信息量，结合变换一致性信号排序实现步长选择，保留重要且多样的token。
result: 在多个VLM基准上，以高压缩率达到与原始模型相当的精度，显著降低计算负担。
conclusion: 本文提出即插即用的无注意力轻量token缩减框架，有效提升VLM在资源受限场景的部署效率。
---

## 摘要
视觉-语言模型（VLM）在多模态理解中取得了强性能，但由于处理大量视觉令牌带来的巨大计算开销，在资源受限的边缘设备上部署仍具挑战。令牌缩减是加速VLM推理的一个有前景的方向，但现有方法要么依赖于与现代加速框架不兼容的注意力图，要么依赖于计算密集型的成对相似性比较，这损害了可扩展性，并抵消了其在实际部署中的优势。本文提出了一种无注意力且轻量化的令牌缩减框架，作为VLM的即插即用模块，该框架通过保留重要且多样的令牌来生成紧凑的视觉表示。首先，为实现无注意力的重要性估计，我们采用信息论视角，利用一种新颖的基于熵的准则量化令牌信息，保留那些具有更强表达能力且退化特征较少的令牌。其次，为以轻量化方式确保视觉覆盖的多样性，我们引入一种变换诱导的一致性信号，其中相似令牌产生相似信号，通过按此信号排序，相似令牌彼此靠近，从而能够采用步长选择来生成多样化的令牌集。在多个VLM基准上的大量实验表明，我们的框架实现了良好的精度-效率权衡，在激进压缩下仍保持有竞争力的性能。

## Abstract
Vision-Language Models (VLMs) have achieved strong performance in multimodal understanding, yet remain challenging to deploy on resource-constrained edge devices due to the substantial computational overhead of processing numerous visual tokens. Token reduction is a promising direction for accelerating VLMs inference, but existing approaches either rely on attention maps that are incompatible with modern acceleration frameworks or depend on computationally intensive pairwise similarity comparisons, which undermine scalability and negate their practical benefits in deployment. In this paper, we propose an attention-free and lightweight token reduction framework as a plug-and-play module for VLMs, which preserves both important and diverse tokens to produce a compact visual representation. First, to enable attention-free importance estimation, we adopt an information-theoretic perspective and quantify token information using a novel entropy-based criterion, retaining those with more expressive and less degenerate feature representations. Second, to ensure diverse visual coverage in a lightweight manner, we introduce a transformation-induced consistency signal where similar tokens yield similar signals, such that sorting by this signal places similar tokens close to each other and enables stride-based selection to produce a diverse token set. Extensive experiments across multiple VLMs benchmarks demonstrate that our framework achieves a favorable accuracy-efficiency trade-off, maintaining competitive performance under aggressive compression.

---

## 论文详细总结（自动生成）

好的，以下是根据你提供的论文内容生成的详细中文总结。

### 论文中文总结：面向高效视觉-语言模型的无注意力且轻量化令牌缩减（ALTR）

#### 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：视觉-语言模型（VLM）在理解图像时需要将图片切分成成百上千个“视觉令牌”，这些Token会送入大语言模型进行自注意力计算。大量Token导致计算复杂度呈平方级增长，造成巨大的计算和内存开销，使得VLM难以部署在资源受限的边缘设备上。
- **现有方法缺陷**：
    - **基于注意力图的方法**：如SparseVLMs、PyramidDrop，依赖注意力图来筛选Token，但它们与现代加速框架（如FlashAttention，其核心思想就是不显式生成注意力图）不兼容，因此反而限制了推理加速。
    - **基于相似度比较的方法**：如SAINT，通过计算Token之间的成对相似度来剔除冗余Token，但此类方法本身计算量过大（通常是O(N²)或O(N³)复杂度），削弱了令牌缩减的效率优势。
- **研究动机**：设计一种**不依赖注意力图**且**计算成本低**的令牌缩减方法，同时与FlashAttention等现代加速框架兼容，以真正实现高效率的VLM边缘端部署。

#### 2. 论文提出的方法论
- **核心思想**：提出一个名为 **ALTR** 的即插即用框架，该方法在视觉编码器与大语言模型之间，通过**一次性选择**（one-shot selection）的方式，联合考虑**令牌重要性**和**令牌多样性**，以生成一个紧凑且信息丰富的视觉令牌子集。该方法训练-免费，且不依赖注意力图。
- **关键技术细节**：
    - **阶段一：基于Rényi熵的重要性感知令牌选择**
        - **目标**：筛选出信息含量最丰富的视觉令牌。
        - **方法**：将每个令牌的特征向量归一化为一个概率分布（表示其在不同语义成分上的激活程度）。使用**二阶Rényi熵**（`H₂(xᵢ) = -log(∑pⱼ²)`）来衡量这个分布的集中度。
        - **原理**：熵值高的令牌，其特征激活在多个维度上分布更均匀，意味着它编码了更丰富、更非退化的语义信息。
        - **操作**：计算所有令牌的Rényi熵，选择熵值最高的 `K₁` 个令牌，作为`X_imp`。
    - **阶段二：基于特征变换一致性的多样性感知令牌选择**
        - **目标**：从剩余令牌中挑选出视觉内容多样的令牌，避免选择语义相似的冗余Token。
        - **方法**：利用视觉编码器最后一层MLP模块。对于每个剩余令牌，分别获取其经过MLP前后的表示（`x'_i` 和 `x''_i`），计算它们的**余弦相似度**（`sᵢ = cos(x'_i, x''_i)`）。
        - **原理**：内容相似的令牌，在经过MLP变换时，其表示的改变模式（即相似度值）也会相似。这意味着按照此相似度值对剩余令牌**进行排序**，内容相似的令牌会被聚拢到一起。
        - **操作**：对排序后的令牌列表进行**步长采样**（stride-based sampling），均匀选取 `K₂` 个令牌，从而保证覆盖不同视觉区域，生成`X_div`。
    - **整体流程**：
        1.  设定目标保留Token总数 `K` 和平衡参数 `λ`。
        2.  计算 `K₁ = λK`，`K₂ = (1-λ)K`。
        3.  执行阶段一，得到`X_imp`。
        4.  在 `X \ X_imp` 上执行阶段二，得到`X_div`。
        5.  最终输出的精简令牌集 `X* = X_imp ∪ X_div`。

#### 3. 实验设计
- **使用数据集与基准（Benchmark）**：
    - **主要评测模型**：LLaVA-v1.5-7B。
    - **评测基准**：MME (总分)、TextVQA (准确率)、ScienceQA (准确率)、GQA (准确率)。这些基准覆盖了感知、OCR、科学推理和组合视觉推理等不同能力。
    - **扩展验证模型**：LLaVA-Next（高分辨率模型）和 Qwen2.5-VL。
    - **压缩比设置**：将原本的576个视觉令牌分别减少到 **192个（降低66.7%）**、**128个（降低77.8%）** 和 **64个（降低88.9%）**。
- **对比方法**：
    - **Inner-LM（在语言模型内部缩减）**：SparseVLMs, PyramidDrop, MustDrop.
    - **Before-LM（在送入语言模型前缩减）**：VisionZip, VisPruner.

#### 4. 资源与算力
- 文中明确提及的实验硬件为**单个NVIDIA RTX 5880 GPU**（用于效率分析）和**NVIDIA Jetson AGX Orin**（用于边缘端部署可行性测试）。
- **关于训练时长和GPU总数**：论文未明确说明。由于该方法宣称是“训练-免费”（training-free），它可能不涉及模型训练，因此未给出训练算力相关信息。

#### 5. 实验数量与充分性
- **实验数量**：较为充分。主要包括以下几类：
    - **主实验**：在LLaVA-v1.5上，针对3种压缩比（192、128、64个Token），在4个Benchmark上，与5种基线方法进行对比。
    - **扩展性实验**：在LLaVA-Next和Qwen2.5-VL上验证了方法的泛化能力。
    - **效率分析**：对比了不同方法的计算复杂度、FLOPs和CUDA时间。
    - **兼容性实验**：测试了方法在启用FlashAttention-2时的性能。
    - **边缘端测试**：在Jetson AGX Orin上进行了内存界限（OOM boundary）测试。
    - **消融实验**：对重要性模块和多样性模块进行独立贡献分析。
    - **超参数分析**：对平衡参数 `λ` 在不同压缩比下的影响进行敏感性分析。
- **充分性与客观性评价**：
    - **优点**：实验设计比较全面，不仅考虑了标准性能，还考虑了实际部署的**效率、兼容性、内存约束**等关键问题，具有很强的工程实践价值。
    - **不足**：
        1.  **对比的公平性**：论文对比了不同类别的基线方法，但在消融实验中未能与具体的技术组合（如“熵+随机采样”、“相似度+步长采样”）进行对比，未能完全凸显其组合设计的优越性。
        2.  **任务局限性**：所有评测任务均为问答和评估类。缺乏在生成类任务（如图像描述生成）上的性能数据，可能限制了其泛化结论的有效性。

#### 6. 论文的主要结论与发现
- **性能卓越**：ALTR在维持极高性能的同时实现了大量视觉令牌缩减。例如，在LLaVA-1.5上，仅保留 **192个Token（降低66.7%）** 时，平均性能保持率达到 **98.86%**；即使在最严格的 **64个Token（降低88.9%）** 下，性能保持率也有 **94.52%**，在所有压缩比下均超越了所有对比方法。
- **效率极高**：ALTR的计算复杂度仅为 `O(N D + N log N)`，远低于基于相似度比较的方法（VisionZip为`O(N²D)`，VisPruner为`O(N³D)`）。FLOPs和CUDA耗时均为最低。
- **完美兼容**：ALTR的无注意力设计使其能够与FlashAttention-2等现代加速框架无缝结合，实现显著的端到端加速。
- **部署友好**：经边缘测试平台（Jetson AGX Orin）验证，ALTR能有效扩展模型在内存受限下的无内存错误（OOM-free）生成长度，且引入的计算开销远小于其它方法。

#### 7. 优点
- **方法新颖性**：提出了一个巧妙的“无注意力”令牌缩减思路。通过利用 **Rényi熵** 和 **MLP变换一致性** 这两个**内在**特征，而非依赖昂贵的交叉注意力计算，从而绕开了与现代加速框架的不兼容问题。
- **理论与实践结合紧密**：论文不仅有清晰的理论支撑（信息论视角），还实验验证了其与FlashAttention的兼容性以及在边缘设备上的实际效果，体现了扎实的工程功底。
- **计算开销低**：对比方法，其计算复杂度最低，是真正的“轻量化”解决方案，在推理效率提升方面具有明显优势。
- **即插即用性**：作为“训练-免费”的模块，可以直接插入到现成的预训练VLM中使用，无需重新训练或微调，降低了使用门槛。

#### 8. 不足与局限
- **缺乏训练数据或架构扩展的讨论**：论文未讨论该方法是否适用于大规模预训练阶段的令牌削减，也未探讨其与其它训练（如指令微调、视觉指令学习）的结合。目前仅适用于推理阶段。
- **超参数敏感性未完全解决**：虽然文中展示了ALTR在不同 `λ` 值下表现稳定，但并未给出一个在所有场景下都最优的默认值是未明确的。用户可能需要针对不同任务和模型进行小范围的调参。
- **实验结果均为相对值**：论文在汇报性能时大多使用相对原始模型的百分比。虽然方便对比，但也掩盖了任务本身绝对精度的下降，可能给人一种性能损失很小的错觉（例如，在MME基准上从1861.59降到1699.18，绝对分差仍有约160分）。
- **内存带宽瓶颈分析缺失**：在边缘设备上，令牌缩减虽然减少了计算量，但动态选取Token的过程可能引入非连续的内存访问，造成内存带宽瓶颈，而文中未对此进行分析。

（完）
