---
title: Approximate Attention Weighting for Sustainable FPGA-Based Vision Transformer Inference
title_zh: 面向可持续FPGA视觉Transformer推理的近似注意力加权方法
authors: "Muhammad Usman, Muhammad Akmal Shafique, Shujaat Khan, Dorit Merhof"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01798v2"
tags: ["query:mec-video"]
score: 8.0
evidence: 用于边缘视觉检测的FPGA视觉Transformer推理
tldr: "视觉Transformer在边缘视觉检测中优势显著，但其softmax阶段的指数计算与归一化在FPGA上硬件成本高昂。本文提出一种无BRAM的近似注意力加权单元，采用16段分段线性函数近似自然指数，完全基于分布式LUT逻辑实现。在Zynq-7020上，完整注意力行核心仅使用1444个LUT和77个DSP，不占用BRAM，与精确softmax相比top-1准确率差异低于0.20%。该设计为资源受限边缘AI平台上的高效ViT推理提供了低功耗、低面积的可行方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01798v2/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1678, \"height\": 646, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01798v2/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1079, \"height\": 741, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01798v2/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1704, \"height\": 432, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01798v2/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1685, \"height\": 587, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01798v2/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1712, \"height\": 457, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01798v2/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1714, \"height\": 442, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01798v2/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1715, \"height\": 229, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01798v2/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1719, \"height\": 963, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01798v2/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1711, \"height\": 261, \"label\": \"Table\"}]"
motivation: ViT在FPGA上部署时，softmax的指数和归一化计算消耗大量BRAM或CORDIC资源，难以适用于小型边缘设备。
method: 提出BRAM-free近似注意力加权单元，用16段分段线性函数逼近自然指数，保留预训练注意力温度，仅用分布式LUT实现。
result: "在Zynq-7020上核心消耗1444 LUTs、77 DSPs，无BRAM，硬件仿真显示与精确softmax的top-1准确率差小于0.20%。"
conclusion: 该近似方法在显著降低硬件资源的同时保持高精度，适合资源受限的FPGA边缘AI推理场景。
---

## 摘要
视觉Transformer通过使用自注意力捕捉图像区域的全局上下文，重塑了计算机视觉领域。这使得它们在可再生能源基础设施、工业质量控制、医学成像和自主系统感知等应用的边缘视觉检测与监控中具有吸引力。然而，在小型FPGA上部署ViT仍然具有挑战性，因为自注意力中的softmax阶段需要指数运算和归一化，这在硬件中代价高昂。现有实现通常依赖CORDIC流水线或基于BRAM的查找表，增加了面积和功耗。本文提出了一种无需BRAM的近似注意力加权单元，用于基于FPGA的ViT推理。所提出的设计通过一个完全由分布式LUT逻辑实现的16段分段线性函数来近似softmax中的自然指数。与基数为2的近似不同，自然指数公式保留了预训练的注意力温度，并避免了特定模型的重新校准。在Xilinx Zynq-7020上实现后，完整的注意力行核心使用了1444个LUT、77个DSP，且无需BRAM，而硬件精确仿真显示，在ViT系列模型上，其与精确softmax参考的绝对top-1精度差异在0.20%以内。这些结果表明，所提出的核心在资源受限的边缘AI平台上实现节能的ViT推理具有潜力。

## Abstract
Vision Transformers have reshaped computer vision by using self-attention to capture global context across image regions. This makes them attractive for edge visual inspection and monitoring in applications such as renewable-energy infrastructure, industrial quality control, medical imaging, and autonomous-system sensing. However, deploying ViTs on small FPGAs remains challenging because the softmax stage in self-attention requires exponential evaluation and normalization, which are costly in hardware. Existing implementations often rely on CORDIC pipelines or BRAM-based look-up tables, increasing area and power consumption. This paper presents a BRAM-free approximate attention-weighting unit for FPGA-based ViT inference. The proposed design approximates the natural exponential in softmax using a 16-segment piecewise-linear function implemented entirely with distributed LUT fabric. Unlike base-2 approximations, the natural-exponential formulation preserves the pre-trained attention temperature and avoids model-specific recalibration. Implemented on a Xilinx Zynq-7020, the complete attention-row core uses 1444 LUTs, 77 DSPs, and no BRAM, while hardware-accurate emulation shows accuracy within a \(0.20\%\) absolute top-1 difference from the exact-softmax reference on ViT-family models. These results demonstrate the potential of the proposed core for energy-efficient ViT inference on resource-constrained edge-AI platforms.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **研究背景**：Vision Transformer（ViT）在计算机视觉任务中表现优异，尤其适合边缘视觉检测与监控（如可再生能源基础设施、工业质量检测、医学成像、自主系统感知等）。但将ViT部署到资源有限的FPGA（特别是小型FPGA）上面临挑战，因为自注意力中的softmax阶段需要指数运算和归一化，在硬件中实现成本高昂。
- **核心问题**：现有FPGA实现通常依赖CORDIC流水线或基于BRAM的查找表，导致面积和功耗显著增加。基数为2的近似方法会改变预训练模型的注意力温度，需要重新校准。亟需一种低资源、低功耗且保留预训练语义的softmax硬件实现。
- **整体含义**：提出一种无BRAM的近似注意力加权单元，直接近似自然指数，在保持高精度的同时大幅降低硬件资源消耗，为资源受限边缘AI平台上的能效ViT推理提供可行方案。

## 2. 方法论：核心思想、技术细节与算法流程
- **核心思想**：用一段16段均匀分段线性（PWL）函数近似自然指数 \(e^x\)，范围限制在 \([-8, 0]\)（因为softmax经过max-centered处理后输入非正）。该近似完全基于分布式LUT逻辑实现，不使用BRAM。
- **关键技术细节**：
  - 分段边界：\(b_i = -8 + 0.5i, i=0,...,16\)，共17个端点。
  - PWL公式：\(\tilde{e}(x) = y_i + \alpha (y_{i+1} - y_i)\)，其中 \(y_i = e^{b_i}\) 为存储的边界值，\(\alpha \in [0,1)\) 是段内分数偏移。
  - 输入 \(x\) 表示为Q8.8定点格式，首先裁剪到 \([-8,0]\)；段索引和小数偏移从定点位域提取；边界值以定点分数格式存储（共272位），无需BRAM。
  - 保留自然指数形式，避免基-2近似引入的隐式温度变化，无需模型特定校准。
- **数据流**：五级流水线：分数计算（DSP MAC）→ PWL指数加权 → 分母累加 → 值加权分子累加 → 最终恢复除法。采用两遍调度：第一遍计算所有点积分数并记录行最大值；第二遍再次遍历每个token，计算近似权重并累加分子/分母。

## 3. 实验设计：数据集、基准与对比方法
- **数据集**：使用Imagenette（ImageNet的子集，共3550张验证图像）进行精度评估。不进行微调，直接加载预训练权重（来自timm库）。
- **基准设置**：
  - **INT8量化评估**：对ViT-B/16和DeiT-S/16进行Q、K、V的每张量对称伪量化，对比精确FP32、精确INT8、近似FP32、近似INT8四种模式下的top-1精度、余弦相似度和top-1一致性。
  - **INT16匹配协议**：按照Li et al.（2023）的方法，使用INT16量化评估ViT-S/16、ViT-B/16、ViT-L/16，比较FP32精确、INT16精确和RTL-PWL INT16的top-1精度差异。
- **对比方法**：与现有FPGA softmax和注意力硬件进行资源、性能、精度对比，包括Wasef & Rafla (2021)、Koca et al. (2023)、Mehra et al. (2023)、Li et al. (2023)、Hirayae et al. (2025)、ViTA (2023)、Celep et al. (2025)、Aboagye et al. (2026)等。

## 4. 资源与算力
- **FPGA平台**：Xilinx Zynq-7020（xc7z020clg484-1），100MHz。
- **硬件资源**：完整197-token注意力行核心（dk=64, dv=64）占用1444 LUTs、1899 FFs、77 DSPs，零BRAM。其中PWL权重模块仅用71 LUTs、2 DSPs。
- **功耗**：SAIF驱动后仿功率分析显示动态功耗21 mW，总片上功耗124 mW（含静态），动态能效601.3 krows/s/W。
- **延迟与吞吐**：每行79.2μs（7920周期），单核行速率12.63 krows/s，动态每行能耗1.66 μJ。
- **训练资源**：论文未涉及训练，仅使用预训练模型进行推理评估。未提及GPU型号或训练时长。

## 5. 实验数量与充分性
- **实验组数**：
  - 精度评估：INT8设定下2个模型（ViT-B/16、DeiT-S/16）× 4种模式（精确FP32、精确INT8、近似FP32、近似INT8）= 8个精度点。
  - INT16匹配协议：3个模型（ViT-S/16、ViT-B/16、ViT-L/16）各3种模式 = 9个精度点。
  - 硬件对比：与8项现有工作对比资源、性能、精度。
  - 功率分析：完整SAIF切换活动分析。
  - 部署能耗场景：理论推演500节点年能耗。
- **充分性评价**：实验覆盖了不同模型大小、不同量化精度，并进行了硬件精确仿真和对比，基本客观。但仅使用Imagenette单一数据集，未在完整ImageNet或真实应用数据集上验证；未做消融实验（如不同分段数S的效果仅用理论曲线展示，未实际评估对精度影响）；INT16协议下与Li et al.的对比因数据集不同不能直接排名，但作为稳健性检查有效。

## 6. 主要结论与发现
- 所提方法在Zynq-7020上实现了零BRAM的完整注意力行核心，资源消耗极低（2.71% LUTs，35% DSPs）。
- 与精确softmax相比，INT16评估下top-1精度最大差异仅-0.20%（ViT-S/16），ViT-B/16和ViT-L/16差异分别在-0.03%和+0.05%，可忽略。
- 动态功耗仅21 mW，远低于对比的35 mW（Wasef & Rafla），且无需BRAM。
- 直接近似自然指数避免了温度校准，保持预训练模型语义。
- 500节点持续监测场景下，相对10W嵌入式GPU参考，每年可节省约43.26 MWh能量（约17.74吨CO₂）。

## 7. 优点（方法或实验设计亮点）
- **资源高效**：PWL权重模块仅71 LUTs，整个核心仅1444 LUTs、77 DSPs、零BRAM，非常适合资源极度受限的SoC FPGA。
- **无需校准**：直接近似自然指数而非基-2，保留预训练注意力温度，简化部署流程。
- **精度保持**：最大0.20% top-1下降在大模型上甚至略有提升（+0.05%），说明PWL近似对最终分类影响极小。
- **硬件可部署性强**：采用两遍调度和分布式LUTRAM实现，无CORDIC或BRAM依赖，综合后时序裕量充足（WNS +1.50ns）。
- **功耗评估严谨**：使用SAIF切换活动文件进行后仿功耗分析，保证≥95%覆盖，得到可靠的动态和总功耗数据。
- **部署场景量化**：通过500节点年能耗示例，直观展示能效优势，强调可持续性。

## 8. 不足与局限
- **实验结果覆盖单一**：精度评估仅使用Imagenette（约10类），未在完整ImageNet-1K或其他真实边缘视觉任务（如光伏板缺陷分割、风力预测）上验证，可能存在类间偏差。
- **缺乏消融实验**：未实际对比不同分段数（如S=16 vs S=32）对精度和资源的具体影响，仅给出理论误差曲线。
- **未考虑完整Transformer流水线**：仅实现了单头注意力行，缺少QKV投影、层归一化、多层堆叠等，实际系统功耗和资源可能显著增加。
- **INT8实验精度对比不完整**：INT8下未报告与INT16相同的细粒度top-1差异（仅给出与精确INT8的差异<0.2%），且余弦相似度约0.988，可能非单位输出带来后续层误差累积。
- **对比方法的公平性**：对比时部分工作仅实现softmax模块而非完整注意力行，且目标设备不同（如UltraScale+ vs Zynq-7020），资源比较存在边界差异。
- **功耗估计的局限性**：仅考虑算术核心功耗，未包括外部内存、DMA、传感器接口等，真实部署时总功耗可能数倍于此。
- **扩展性未验证**：对于更大序列长度（如N>197）或更高维度（如dk=128）的吞吐和资源开销未讨论。

（完）
