---
title: Approximate Attention Weighting for Sustainable FPGA-Based Vision Transformer Inference
title_zh: 用于可持续FPGA视觉Transformer推理的近似注意力加权
authors: "Muhammad Usman, Shujaat Khan, Dorit Merhof"
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.01798v1"
tags: ["query:mec-video"]
score: 8.0
evidence: 边缘视频分析中FPGA上高效ViT推理
tldr: "Vision Transformer在边缘设备部署时，自注意力中的softmax需要指数运算和归一化，硬件成本高。本文提出无BRAM的近似注意力加权单元，用16段分段线性函数逼近自然指数，全部基于分布式LUT实现。在Zynq-7020上，核心仅消耗1444 LUTs和77 DSPs，top-1精度与精确softmax相差0.20%。该方法避免了模型重新校准，为资源受限的FPGA提供了高效节能的ViT推理方案。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.01798v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1676, \"height\": 645, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01798v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1079, \"height\": 740, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01798v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1705, \"height\": 434, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.01798v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1684, \"height\": 585, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.01798v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1714, \"height\": 441, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01798v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1713, \"height\": 455, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01798v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1711, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01798v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1719, \"height\": 969, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.01798v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1712, \"height\": 259, \"label\": \"Table\"}]"
motivation: ViT的softmax在FPGA上实现需要大量硬件资源，现有CORDIC或BRAM方案导致面积和功耗过高。
method: 采用16段分段线性函数近似自然指数，全部通过分布式LUT实现，无需BRAM，保留预训练温度参数。
result: "在Zynq-7020上，注意力核心使用1444 LUTs、77 DSPs，无BRAM，top-1精度损失仅0.20%。"
conclusion: 该近似单元适用于资源受限边缘AI平台，实现高能效ViT推理且无需模型特定校准。
---

## 摘要
视觉Transformer通过使用自注意力机制捕获图像区域的全局上下文，重塑了计算机视觉领域。这使得它们在可再生能源基础设施、工业质量控制、医学成像和自主系统传感等应用中的边缘视觉检测和监控方面具有吸引力。然而，在小型FPGA上部署ViT仍然具有挑战性，因为自注意力中的softmax阶段需要指数评估和归一化，这在硬件上成本较高。现有实现通常依赖CORDIC流水线或基于BRAM的查找表，增加了面积和功耗。本文提出了一种无BRAM的近似注意力加权单元，用于基于FPGA的ViT推理。该设计使用完全由分布式LUT逻辑实现的16段分段线性函数来近似softmax中的自然指数。与以2为底的近似不同，自然指数公式保留了预训练的注意力温度，避免了特定模型的重新校准。在Xilinx Zynq-7020上实现后，完整的注意力行核心使用了1444个LUT、77个DSP，且无需BRAM。硬件精确仿真显示，在ViT系列模型上，与精确softmax参考相比，top-1准确率绝对差异在0.20%以内。这些结果证明了所提出的核心在资源受限的边缘AI平台上实现节能ViT推理的潜力。

## Abstract
Vision Transformers have reshaped computer vision by using self-attention to capture global context across image regions. This makes them attractive for edge visual inspection and monitoring in applications such as renewable-energy infrastructure, industrial quality control, medical imaging, and autonomous-system sensing. However, deploying ViTs on small FPGAs remains challenging because the softmax stage in self-attention requires exponential evaluation and normalization, which are costly in hardware. Existing implementations often rely on CORDIC pipelines or BRAM-based look-up tables, increasing area and power consumption. This paper presents a BRAM-free approximate attention-weighting unit for FPGA-based ViT inference. The proposed design approximates the natural exponential in softmax using a 16-segment piecewise-linear function implemented entirely with distributed LUT fabric. Unlike base-2 approximations, the natural-exponential formulation preserves the pre-trained attention temperature and avoids model-specific recalibration. Implemented on a Xilinx Zynq-7020, the complete attention-row core uses 1444 LUTs, 77 DSPs, and no BRAM, while hardware-accurate emulation shows accuracy within a \(0.20\%\) absolute top-1 difference from the exact-softmax reference on ViT-family models. These results demonstrate the potential of the proposed core for energy-efficient ViT inference on resource-constrained edge-AI platforms.

---

## 论文详细总结（自动生成）

# 论文总结：Approximate Attention Weighting for Sustainable FPGA-Based Vision Transformer Inference

## 1. 核心问题与整体含义（研究动机与背景）
- **核心问题**：Vision Transformer（ViT）在FPGA边缘设备部署时，自注意力机制中的softmax阶段需要指数运算和归一化，传统实现依赖CORDIC流水线或BRAM查找表，导致硬件面积大、功耗高、BRAM资源紧张。
- **整体含义**：本文旨在提供一种低资源、低功耗且无需模型重新校准的近似softmax硬件方案，使得ViT推理能在资源受限、可持续的边缘AI平台（如光伏面板检测、风力发电监控等）上高效运行。

## 2. 方法论
- **核心思想**：用16段均匀分段线性（PWL）函数直接近似自然指数 \(e^x\)，替代softmax中的精确指数计算；所有系数存储于分布式LUT，实现零BRAM占用。
- **关键技术细节**：
  - 输入分数 \(s_j\) 先减去行最大值得到 \(u_j \leq 0\)，限定近似区间 \([-8, 0]\)（小于-8的输入饱和处理）。
  - 16个均匀段，边界点 \(b_i = -8 + 0.5i\)（\(i=0,\dots,16\)），每个段内线性插值：\(\tilde{e}(x) = y_i + \alpha (y_{i+1} - y_i)\)，其中 \(y_i = e^{b_i}\)，\(\alpha\) 为段内分数偏移。
  - 17个边界值（每值16位）共272 bits，存储在分布式LUTRAM中。
- **算法流程（文字说明）**：
  1. Pass 1：计算所有点积分数 \(s_j\)，并跟踪行最大值。
  2. Pass 2：对每个token计算 \(u_j = s_j - \max\)，利用16段PWL得到近似权重 \(\tilde{e}(u_j)\)，同时累加分母 \(\sum \tilde{e}(u_l)\) 和分子 \(\sum \tilde{e}(u_j) \cdot v_j\)。
  3. 用串行恢复除法器对每个输出维度做归一化。

## 3. 实验设计
- **数据集**：Imagenette（ImageNet子集，3550张验证图片）。
- **基准（Benchmark）**：
  - 精确FP32 softmax
  - 精确INT8/INT16软sim softmax
  - 对比Li et al. [14]的base-2 Maclaurin近似（论文引用其ImageNet-1K结果）
- **对比方法**：文献中多个现有FPGA softmax单元（Wasef & Rafla, Koca et al., Mehra et al., Li et al., Hirayae et al., ViTA, Celep et al., Aboagye et al.），但本文主要报告自己与精确softmax的对比，而非直接硬件复现对比。
- **模型**：ViT-S/16, ViT-B/16, ViT-L/16, DeiT-S/16（预训练权重来自timm框架，无微调）。
- **精度设置**：INT8（Q、K、V per-tensor对称量化）及INT16（匹配Li et al.的协议）。

## 4. 资源与算力
- **硬件**：Xilinx Zynq-7020（xc7z020clg484-1），100 MHz主频，仅部署推理核心。
- **资源占用**：1444 LUTs, 1899 FFs, 77 DSPs, 0 BRAM（完整注意行核心）。
- **功耗**：动态21 mW，总片功耗124 mW（SAIF驱动后布线估计）。
- **吞吐**：每行79.2 μs，约12.63 krows/s。
- **训练**：论文未涉及训练，所有实验使用预训练模型推理。未报告GPU型号或训练时长。

## 5. 实验数量与充分性
- **主要实验组数**：
  - 两组精度测试：INT8量化（Table 2）和INT16匹配测试（Table 3）。
  - 硬件资源对比表（Table 4）列出多项先前工作，但未在同一平台直接复现对比。
  - 误差/分段数消融（Figure 2）：对比S=16 vs S=32的误差。
- **充分性评价**：
  - **优点**：覆盖了ViT系列多个模型尺寸（S/B/L）和不同精度设置（FP32、INT8、INT16），并报告了余弦相似度和top-1一致率。
  - **不足**：仅使用单数据集Imagenette（小规模），未在ImageNet-1K或其他视觉任务上验证；未评估多头部并行或完整Transformer流水线；未与文献中的近似方法在同一FPGA上做对照实验（仅引用其数字）；缺乏消融实验考察不同分段数对精度-资源权衡的详细影响（仅给出最大误差与S的关系）。

## 6. 主要结论与发现
- 提出的16段PWL自然指数近似在INT16下，ViT-S/B/L的top-1准确率损失不超过0.20%（相比FP32精确softmax）。
- 硬件实现具有零BRAM、极低LUT开销（权重单元仅71 LUTs），完整注意行核心功耗仅124 mW。
- 相比base-2近似，无需模型温度校准，保留了预训练语义。
- 在500节点部署场景下，相比10W嵌入式GPU可年省43.26 MWh能量（约17.74吨CO₂）。

## 7. 优点
- **硬件高效**：完全无BRAM，利用分布式LUT，适合资源受限SoC FPGA。
- **精度保持**：直接近似自然指数，避免base-2转换导致的温度偏移，无需后训练校准。
- **低功耗**：动态功率仅21 mW（权重单元仅3 mW），总片功耗124 mW，效率达601.3 krows/s/W。
- **可迁移性**：方法独立于具体ViT变体，可直接替换softmax模块。

## 8. 不足与局限
- **实验覆盖**：仅使用Imagenette（小数据集），未在ImageNet-1K全量或COCO等任务上验证；未评估目标检测、分割等更复杂ViT应用。
- **对比公平性**：与先前工作的比较基于文献引用数字，未在同一FPGA平台或相同条件下复现，可能存在实现差异。
- **系统级局限**：仅实现了单头注意行算术核心，未包含Q/K/V投影、其他层（如MLP、LayerNorm）及完整流水线；功耗为后布线估计而非板级实测。
- **精度风险**：在极端输入分布下（如热度极高的softmax），PWL近似误差可能累积；INT8实验显示余弦相似度最低至0.988，可能有0.2%精度损失。
- **可扩展性**：仅评估197 token（ViT标准序列长度），更大序列（如视频帧）时近似区间是否充分未讨论。

（完）
