---
title: Profiling-Driven Adaptive Distributed Transformer Inference on Embedded Edge Deployment
title_zh: 基于性能分析的嵌入式边缘部署自适应分布式Transformer推理
authors: "Muhammad Azlan Qazi, Alexandros Iosifidis, Qi Zhang"
date: 2026-05-25
pdf: "https://arxiv.org/pdf/2605.25682v1"
tags: ["query:mec-video"]
score: 8.0
evidence: 在嵌入式边缘设备上分布式Transformer推理
tldr: "嵌入式边缘设备上分布Transformer推理受限于CPU-GPU数据暂存开销，导致全张量交换比单设备更慢。本文提出Prism方法，通过Segment Means压缩与轻量离线性能分析，自适应选择本地或分布式执行。实验表明，相比静态全张量交换，延迟降低65%-77%，能耗减少34%-52%。该工作揭示了性能分析驱动的自适应对嵌入式分布式推理的实际必要性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2605.25682v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 871, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25682v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 704, \"height\": 630, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25682v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 779, \"height\": 589, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25682v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1812, \"height\": 514, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25682v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 866, \"height\": 506, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25682v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1831, \"height\": 538, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2605.25682v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 860, \"height\": 806, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2605.25682v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 883, \"height\": 315, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25682v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 853, \"height\": 899, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25682v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 885, \"height\": 299, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2605.25682v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 773, \"height\": 366, \"label\": \"Table\"}]"
motivation: 现有分布式Transformer推理研究多基于仿真，忽略硬件通信开销，实际部署效果不明。
method: Prism结合Segment Means压缩与离线profiling，运行时自适应选择本地或分布式执行。
result: "相比静态全张量交换，延迟降低65%-77%，能耗降低34%-52%。"
conclusion: 性能分析驱动的自适应策略是嵌入式分布式Transformer推理的关键。
---

## 摘要
在嵌入式边缘设备之间分布Transformer推理可以缓解单个设备的存储和计算限制，但实际硬件上的实用优势尚不明确：先前的工作主要依赖模拟，忽视了硬件特定的通信开销。我们展示了在通过WiFi连接的NVIDIA Jetson Orin Nano设备上的硬件原型研究。我们的主要发现是，主要瓶颈不仅是网络带宽，还包括通信期间的CPU-GPU数据中转。由于Jetson的集成GPU架构缺乏NCCL所需的PCIe/NVLink通道，所有设备间的数据通信应通过GLOO路由并在CPU内存中中转；这种开销随通信数据量增加而增加，使得对于ViT等中等规模模型，在批量大小下全张量交换比单设备推理更慢。因此，我们评估了Prism，它将Segment Means压缩与轻量离线性能分析相结合，在运行时自适应选择本地或分布式执行。实验表明，相对于静态分布式执行设置中的全张量交换，该策略将延迟降低了65%-77%，能耗降低了34%-52%，表明基于性能分析的自适应对于嵌入式硬件上的实际分布式Transformer推理至关重要。

## Abstract
Distributing Transformer inference across embedded edge devices can alleviate individual memory and compute constraints, yet practical benefits on real hardware remain unclear: prior work relies largely on simulations that overlook hardware-specific communication overheads. We present a hardware prototype study on NVIDIA Jetson Orin Nano devices connected over WiFi. Our key finding is that the dominant bottleneck is not just network bandwidth but also the CPU-GPU staging during communication. Because Jetson's integrated GPU architecture lacks the PCIe/NVLink pathway that NCCL requires, all inter-device data communication should be routed through GLOO and staged in CPU memory; an overhead that scales with communication data volume and makes full-tensor exchange slower than single-device inference across the batch sizes for medium sized models such as ViT. We therefore evaluate Prism by combining Segment Means compression with lightweight offline profiling to adaptively select between local and distributed execution at runtime. Experiments show that this strategy reduces latency by 65%-77% and energy consumption by 34%-52% relative to full-tensor exchange in static distributed execution setup, demonstrating that profiling-driven adaptation is essential for practical distributed Transformer inference on embedded hardware.

---

## 论文详细总结（自动生成）

# 论文总结：Profiling-Driven Adaptive Distributed Transformer Inference on Embedded Edge Deployment

## 1. 论文的核心问题与整体含义（研究动机和背景）
- **核心问题**：在嵌入式边缘设备（如NVIDIA Jetson Orin Nano）上分布式Transformer推理时，由于硬件限制（集成GPU缺乏PCIe/NVLink通道），全张量交换方法（如Voltage）的性能甚至不如单设备推理。现有研究多基于仿真，忽略了CPU-GPU数据中转（staging）这一实际瓶颈。
- **整体含义**：论文通过硬件原型实验揭示，分布式推理的主要瓶颈不是网络带宽，而是GLOO后端导致的CPU-GPU数据中转开销。作者提出Prism方法，利用Segment Means压缩和离线性能分析（profiling），在运行时自适应选择本地或分布式执行，从而显著降低延迟和能耗。该工作强调了实际硬件原型验证和性能分析驱动自适应策略对于嵌入式边缘部署的重要性。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程
- **核心思想**：
  - 采用位置划分（position-wise partitioning）将输入序列切分到多个设备，每个设备独立处理其分区。
  - 用紧凑的Segment Means替代全张量中间特征进行设备间通信，大幅减少通信数据量。
  - 结合离线性能分析（profiling）建立性能映射表，运行时根据当前批量大小和网络带宽，自适应选择单设备或分布式执行模式，并选取最优压缩率（CR）。
- **关键技术细节**：
  - **Segment Means通信**：将每个设备上的token序列分区进一步划分为L个等长段，计算每段的列平均（column-wise mean），得到压缩表示Z_p ∈ R^{L×D}。每个设备接收其他设备的Segment Means，与自身的完整分区拼接形成增强表示\hat{X}_p。压缩率CR = N/(L·P)，控制通信量与精度权衡。
  - **缩放感知Softmax重表述**：利用公式的重复结构减少冗余的Key/Value重计算，降低设备计算量（可达50.11%）。
  - **离线性能分析**：扫描批量大小B∈{1,2,4,8,16,32}、CR∈{3.3,4.95,9.9}、网络带宽BW∈{200~900 Mbps}，测量延迟、能耗并分解为计算、通信、CPU-GPU I/O三部分，生成JSON格式性能映射表。
  - **运行时决策**：终端设备观测当前B和BW，查询性能映射，选择延迟或能耗最优的执行模式（单设备或分布式+最佳CR）。
- **算法流程**：
  1. 终端设备对输入X进行位置划分，计算各分区的Segment Means。
  2. 每个设备接收本分区和所有其他设备的Segment Means，构建增强表示。
  3. 执行自注意力和FFN计算（利用缩放感知Softmax优化）。
  4. 通过GLOO后端进行各层的同步（仅交换Segment Means）。
  5. 运行时根据离线映射表决定是否使用分布式（若小批量则退回单设备）。

## 3. 实验设计：使用了哪些数据集/场景，它的benchmark是什么，对比了哪些方法
- **数据集**：CIFAR-10（图像分类，输入大小224×224×3，token数N=197）。
- **模型**：ViT（Vision Transformer）。
- **场景**：两个Jetson Orin Nano板通过WiFi 6E连接，部署在本地边缘环境。
- **对比方法**：
  - Prism（提出方法，CR=9.9, 4.95, 3.3）
  - Voltage（全张量交换，baseline分布式方法）
  - 单设备推理（作为下界基线，非分布式）
- **评价指标**：总延迟（ms）、每样本延迟（ms）、每样本能耗（J）、CIFAR-10准确率。还进行了带宽敏感性测试（200-900 Mbps）。

## 4. 资源与算力
- **实验硬件**：2块NVIDIA Jetson Orin Nano开发板（8GB LPDDR5统一内存、1024 Ampere CUDA核心、ARM CPU、Intel AX210 WiFi 6E模块）。通信后端为GLOO（因集成GPU无PCIe/NVLink，NCCL不可用）。
- **训练**：未明确说明训练时长或算力消耗。模型为预训练ViT，论文中提到CR=9.9的准确率从98.01%降至95.64%，但微调后恢复到97.43%，但微调细节（数据、超参数、GPU型号）未详述。
- **推理测量**：每个配置执行20次warm-up后取平均，总profiling次数约为|B|×|CR|×|BW|×20 ≈ 6×3×8×20=2880次推理pass，但文末称“~200次profiling pass”，可能指每个B/BW组合只需少量pass？存在表述差异。

## 5. 实验数量与充分性
- **实验数量**：
  - 主实验：B=1,2,4,8,16,32，共6个批量大小；CR=9.9（主要）、另用3.3和4.95做消融；BW固定约400 Mbps。
  - 带宽敏感性实验：BW=200-900 Mbps（step 100），B=8。
  - 微调准确率：展示了不同CR下的准确率（表3），并说明微调后可恢复。
  - 定性验证：16张CIFAR-10图像的推理结果对比（图5）。
- **充分性判断**：
  - **优点**：覆盖了多个批量大小、压缩率、网络带宽，进行了详细的延迟分解（计算/通信/CPU-GPU I/O），具有硬件原型实测数据，对比了单设备和全张量交换方法。
  - **不足**：仅采用单一模型（ViT）和单一数据集（CIFAR-10），未在语言模型或其他视觉任务上验证；仅使用2个设备，未扩展至更多设备；未与其他轻量方法（如Tensor Parallelism）对比；无统计显著性检验（如多次独立实验的标准差）。但鉴于论文是workshop短文，实验设计相对充分。

## 6. 论文的主要结论与发现
- GLOO的CPU-GPU数据中转是嵌入式分布式Transformer推理的根本瓶颈，使全张量交换方法（Voltage）在所有批量大小下都比单设备推理更慢。
- Prism通过Segment Means减少约90%的通信数据量，能显著降低中转开销，在各批量下延迟降低65%-77%，能耗降低34%-52%。
- 存在批量大小的交叉点（本文为B=8）：低于该点时，固定开销使分布式仍不如单设备，自适应策略可自动回退。
- 网络带宽影响：Prism在较低带宽（约340 Mbps以上）即可优于单设备，而Voltage始终受限于带宽。
- 压缩率与准确率存在权衡：CR=9.9时准确率下降约2.4个百分点（无微调），微调后仅下降0.6个百分点。

## 7. 优点
- **硬件原型验证**：在真实嵌入式设备（Jetson Orin Nano）上进行系统级实测，而非仿真，揭示了被以往工作忽略的CPU-GPU中转瓶颈。
- **方法实用简洁**：Segment Means压缩方案实现简单，无需修改模型架构或训练流程；离线profiling开销小（~200次推理pass），生成JSON映射表存储和查询轻量。
- **自适应策略有效**：通过profiling驱动的自适应避免了静态部署的次优性，能自动权衡计算与通信成本。
- **详细性能拆解**：将延迟分解为计算、通信、CPU-GPU I/O三部分，清晰揭示了瓶颈所在。
- **提出具体设计指南**：如“Always assume GLOO on Jetson”、“Profile; do not estimate”。

## 8. 不足与局限
- **实验覆盖有限**：仅使用ViT和CIFAR-10，未评估其他Transformer架构（如BERT、LLM）或更大输入尺寸；仅2个设备，未扩展到更多节点（如3-4台设备），无法看出扩展性。
- **压缩率选点较少**：CR仅3个值（9.9,4.95,3.3），且主实验固定CR=9.9，对CR的影响缺乏更细粒度探索。
- **未完全消除GLOO中转**：即使使用Prism，残差的中转成本在小批量时仍导致不如单设备，未提出根本性解决（如优化通信后端、支持NCCL或使用直接GPU间通信）。
- **微调细节缺失**：恢复准确率的微调过程未提供超参数、训练步数等，影响可复现性。
- **统计信息不足**：所有测量报告为20次平均值，未给出标准差或置信区间，难以评估变异性。
- **能耗测量方法**：通过jtop读取板载功率监视器，但未说明采样率或校准，可能不精确。
- **现实部署偏差**：WiFi环境可能引入非受控干扰，带宽限制通过tc netem模拟，可能不完全反映真实无线信道变化。

（完）
